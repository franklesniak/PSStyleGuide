<!-- markdownlint-disable MD013 -->
<!-- markdownlint-configure-file { "MD024": { "siblings_only": true } } -->

# Chronological PSStyleGuide-first cross-repository convergence plan

## Purpose

Complete the remaining PSStyleGuide and TerraformStyleGuide convergence work in one verified execution order. The completed history includes the cycle-1 fixed point, the reviewed and merged PS #161 implementation, and the reviewed and merged Terraform #34 cycle-2 repair. The first incomplete action is to validate PS #168 as the self-contained authority for PR-body identity synchronization. Close #168 through its reciprocal fixed point before the ordered cycle-2 reverse comparison. Then close PS #169, #170, and #171 in separate consecutive lifecycles before PS #162 starts.

This plan contains only numbered atomic leaf tasks. Each task has one executor, one primary action or lifecycle gate, and one objectively testable result. Conditional leaves record `SKIPPED — NO_REPAIR` only when their named comparison predecessor emits `NO_REPAIR`. A reciprocal repair loop repeats only the explicitly named issue-through-handoff leaf range as a new instance.

No task depends on a shared execution contract, common lifecycle, common prompt, replacement contract, or common-foundation section. Every operative requirement appears in each task that needs it.

## Execution classes

- `Coding agent executable`: A coding agent can complete the task with the stated tools and authority. A task-local human approval remains a required input when stated.
- `Human execution required`: A repository owner or administrator must make or approve the decision. A coding agent can prepare evidence but cannot supply approval.
- `Not executable; tracking/control only`: The item records a control state and cannot itself authorize implementation. No current task uses this class.

## Execution-order map

| Task range | Ordered result |
| --- | --- |
| 1–28 | Validate, implement, review, merge, compare, and reciprocally close PS #168. |
| 29–57 | Perform the landed cycle-2 reverse comparison; close PS #169, #170, and #171 through separate PR lifecycles and per-PR comparisons; publish the cycle-2 fixed point. |
| 58–85 | Implement, review, merge, and reciprocally close PS #162; post the initial three-cycle handoff. |
| 86–114 | Update, implement, review, merge, and reciprocally close PS #158; close PS #159. |
| 115–142 | Implement, review, merge, and reciprocally close PS #163; close Terraform #31. |
| 143–169 | Implement, review, merge, and reciprocally close Terraform #21. |
| 170–182 | Inventory and close all untracked cross-repository blockers. |
| 183–220 | Prepare, review, decide, prove, land, and reciprocally close P1B/T1B. |
| 221–247 | Implement, review, merge, and close the PS #148 applicability cycle. |
| 248–265 | Implement, review, merge, and close Terraform #23 and its PS applicability result. |
| 266–283 | Prepare and review PS #149, make the fixed-date decision, process any approved fallback, and land P3. |
| 284–311 | Implement, review, merge, and reciprocally close Terraform #24. |
| 312–338 | Implement, review, merge, and close the PS #151 applicability cycle. |
| 339–361 | Check residual triggers, close triggered work, and run later paired cycles when applicable. |
| 362–362 | Publish the final completion audit. |

The only calendar override is Task 271, which runs at `2026-08-21T23:59:59Z`. Its task-local controls define the incomplete-predecessor branch. Every declared task edge is `FS` or a task-local conditional repeat. The read-only PS #152 evidence task has an `FS` edge from Task 185 because Task 186 requires Task 185's completion record.

## Dependency relationship reference

| Code | Relationship | Meaning |
| --- | --- | --- |
| `FS` | Finish-to-start | The predecessor must finish before the successor starts. |
| `FF` | Finish-to-finish | The successor cannot finish before the predecessor finishes. |
| `SS` | Start-to-start | The successor cannot start before the predecessor starts. |
| `SF` | Start-to-finish | The predecessor cannot finish before the successor starts. |

The graph contains no legitimate `SF` relationship. Former finish-to-finish tracker conditions are represented as separate closure tasks.

## Verified current state

This state was re-queried through `2026-08-19T03:57:43Z` with the connected GitHub application, authenticated and paginated GitHub REST and GraphQL reads, Git object reads, and the durable local task tracker. Every state-changing task must re-query its task-local identities before it acts.

### Completed PS #161 lifecycle

- Fresh native Codex readiness, the complete review loop, and an independent final quality check all completed on PR #167 head `769d505d46cc984416c3976ad43f1f800803fa81`, tree `215fc660c63e6c59f38f865faa3a441452c36605`. The final inventory contains 227 reviews, 109 resolved and zero unresolved review threads, 246 inline comments, and 114 PR comments.
- PR #167 merged through a two-parent merge at `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`. Issue #161 is `CLOSED` with reason `COMPLETED`.
- The permanent `READY_FOR_TERRAFORM_COMPARISON` handoff is issue comment `5334317038`, 14,741 UTF-8 bytes, SHA-256 `ec242de49873c0e7c1441d49067cf5d7a2270506e78ece9d70eba2343a4574ef`.
- Final-head checks `validate_agent_instructions`, `verify_generated_artifacts`, `markdownlint`, and `copilot-pull-request-reviewer` succeeded. The landed three main-branch validation checks also succeeded.

### Completed Terraform #34 lifecycle

- The pinned comparison after PS #161 emitted `REPAIR_REQUIRED` for `GF-WRITE` and `GF-FAILURE`. Byte-identical records exist on PS #161 comment `5334477676`, PS #159 comment `5334477846`, and Terraform #31 comment `5334478004`.
- Terraform issue #34 was created as child of tracker #31, separately commenced, implemented, reviewed, independently quality-checked, and merged through PR #35. All six review threads are resolved and zero are unresolved.
- PR #35 reviewed head `1fd03050ca8855438f589b855a360e70105ec632`, tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`, merged at `fe28313e118497e2567bf306b74ec596dd10eb64` with the same landed tree. Issue #34 is `CLOSED` with reason `COMPLETED`.
- The permanent `READY_FOR_PS_COMPARISON` handoff is issue comment `5336853959`, 15,135 UTF-8 bytes, SHA-256 `7961866368e93ba37a20c688f2dc849728290532717127acd0b7adba1ba33e5f`.
- Final-head checks `verify`, `publish`, `policy`, `markdownlint`, and `copilot-pull-request-reviewer` succeeded.

### New PS work items

| Issue | Body identity | Current state and relationships |
| --- | --- | --- |
| [#168](https://github.com/franklesniak/PSStyleGuide/issues/168) | 1,852 UTF-8 bytes; SHA-256 `998fe1c993a6e2fefd933b9a3f6612b193383b8acf704ef0a9d487589597d710` | Open; zero comments or edits; no labels, assignees, milestone, parent, sub-issues, dependencies, linked branch, or closing PR. Cross-referenced by merged PR #167 and closed #161. |
| [#169](https://github.com/franklesniak/PSStyleGuide/issues/169) | 6,843 UTF-8 bytes; SHA-256 `f10c8d7e4dc6b0a7938f8e4f17bf193c7deada650fcf8d2824323375f1e903f6` | Open with the same empty metadata and relationship surfaces. Cross-referenced by merged Terraform PR #35 and closed Terraform #34. Its body requires the ordered landed reverse comparison before implementation. |
| [#170](https://github.com/franklesniak/PSStyleGuide/issues/170) | 4,333 UTF-8 bytes; SHA-256 `59771ad2e7d87d19f4b9a3bf8e8343a7e78d6ef5b51ac9ca00254e85d99dd547` | Open with the same empty metadata and relationship surfaces. Cross-referenced by merged Terraform PR #35 and closed Terraform #34. |
| [#171](https://github.com/franklesniak/PSStyleGuide/issues/171) | 5,171 UTF-8 bytes; SHA-256 `6a2e10e574348a283b60334b4b95ec14d03af71d937799e862460aabdfa9c9c1` | Open with the same empty metadata and relationship surfaces. Cross-referenced by merged Terraform PR #35 and closed Terraform #34. It must preserve PS #162's no-publication boundary. |

PS #168 can close before the landed cycle-2 reverse comparison. PS #169, #170, and #171 run after that comparison because #169 explicitly requires its pinned landed identities. Their separate consecutive PRs preserve exact issue scope and give every merged PR its own comparison.

### Current dependency slate

- PS #159 is open with children #160 and #161 closed and children #162 and #158 open. Native edges remain #160 → #161 → #162. PS #158 remains a nonblocking child.
- PS #147 remains blocked by open administrator issue #152 and closed #146, then blocks #148; the native chain continues #148 → #149 → #151.
- PS #163, #155, #156, and #168–#171 remain open and have no native blocker. Cross-references are not dependency edges.
- Terraform #31 remains open with children #32 and #34 closed. The parent/child tracker relationship is not a blocker.
- Terraform #21 is open with only closed blockers #20 and #28; the native chain continues #21 → #22 → #23 → #24 → #25. Closed #28 also remains a blocker record on #22.
- Current PS `main` is `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`. Current Terraform `main` is `fe28313e118497e2567bf306b74ec596dd10eb64`, tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.

### Immutable current foundation inputs

| Role | PS landed identity | Terraform landed identity |
| --- | --- | --- |
| Canonical commit and tree | `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b` / `215fc660c63e6c59f38f865faa3a441452c36605` | `fe28313e118497e2567bf306b74ec596dd10eb64` / `cc20f596fda26ec7374adc5fe235ea02c80ee151` |
| Generator blob | `e19080ef9108fcdec66b634037b891a94352723d` | `d1c1d4ffffca1f656b6acd4d106d94185680a222` |
| Policy-validator blob | `353332cdbf5060051dd85ee7f5d9b555388206ae` | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` |
| Build-workflow blob | `91523ef04cf512cb0b507d3f6a0239bc9c649ed7` | `e88eb4d43465ca0e5dfb5d1e4c71c53514e7a5db` |
| PS policy-contract blob | `3d7344d0665bfb6f363c9966c59f795ba03baed9` | Not present as a role-equivalent standalone path; prove mapping in each comparison. |
| Root workflow instructions | `AGENTS.md` blob `b43a88f8a32f570e3fc7631cabe1fc21c510d82f`; `CLAUDE.md` blob `cc84e59db916d4efae87ed91989229fedb9f57f8` | No root `AGENTS.md`; root `CLAUDE.md` blob `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` is the compatibility workflow input. |

## Chronological task slate

## Task 1 — validate and update PS #168 for PR-body identity synchronization

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally with the exact value from its named source before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by this task |
| Issue URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing open issue; re-query before execution |
| Planning-baseline body | 1,852 UTF-8 bytes; SHA-256 `998fe1c993a6e2fefd933b9a3f6612b193383b8acf704ef0a9d487589597d710` | Authenticated read at the verified-state timestamp; current GitHub state controls |

### Record inputs

The issue currently has zero comments, body edits, labels, assignees, milestones, dependencies, parent or sub-issues, linked branches, and closing PRs. Re-query every connection and relationship with complete pagination before any write.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Completed PS #161 and Terraform #34 handoffs | `FS` | PR #167 and PR #35 are merged; no implementation issue or PR currently owns the one-at-a-time slot. |

### Objective

Validate the existing issue as the complete, self-contained authority for PR-body identity synchronization. Update only missing or stale issue metadata that current repository and GitHub evidence proves necessary. Do not commence implementation, edit a repository byte, create a branch or PR, post an implementation comment, merge, close the issue, or change a repository setting.

### Execution controls

1. Use authenticated structured GitHub reads and paginate the issue body edits, comments, timeline, parent, sub-issues, blocked-by, blocking, tracked issues, linked branches, and closing references.
2. Re-query both repository `main` refs, all open implementation issues and PRs, and the one-at-a-time implementation slot.
3. Preserve the existing issue body unless a specific statement is false, incomplete, or no longer self-contained. Make one exact issue-body update only when evidence requires it.
4. Add a native parent or dependency only when it is a real completion prerequisite. A cross-reference, tracker membership, shared file, or desired order is not a blocking edge.
5. Keep issue update and implementation commencement separate. This task must not create a feature branch, edit tracked bytes, push, open a PR, or post `IMPLEMENTATION COMMENCED`.
6. Apply the issue-specific scope below. Do not merge this issue with another issue's implementation authority.
7. Read back every authorized change and compare its exact body, relationships, state, and timestamps. Preserve failure truth and stop after this task.

### Issue-specific scope

- Preserve issue #168's exact durability objective: derive every PR-body identity from the checked-out head, compute the canonical policy-contract digest with `validatorIdentity` excluded by the validator's exact algorithm, and emit one deterministic Markdown block for the current head and tree.
- Detect a stale or partial identity block before automated review. Update all coupled generator, path-verifier, build-step, contract, and validator facts as one unit, or fail without changing the PR body.
- Authenticate every PR-body update through an exact readback. Never store credentials, API responses, or unrelated PR text in tracked artifacts.
- Do not change the generator algorithm or exact-path algorithm delivered by PS PR #167. Use a finding-specific decision to select the smallest repository-owned command, validation entry point, fixtures, and documentation paths.
- Require deterministic output, stale-block negative controls, partial-block negative controls, hostile or malformed value controls, exact head/tree binding, a no-change control, repository Markdown validation, and successful current-head CI.
- After the PS handoff, compare applicability in Terraform. Preserve byte-identical common CI behavior except proved repository-specific names and path roles.

### Procedure

1. Capture the complete current issue and repository baseline.
2. Build a requirement-to-body matrix for every scope, exclusion, validation, evidence, relationship, terminal, and reopen condition above.
3. If the body is already complete and current, record `ISSUE_READY_UNCHANGED`. If it is not, validate the defect, use a finding-specific decision process, update only the issue body or real relationship, read it back, and record `ISSUE_READY_UPDATED`.
4. Confirm that no implementation commenced and that the implementation slot remains free.
5. Return the issue URL, node ID, state, body bytes and SHA-256, relationship inventory, cross-references, exact mutation or no-change result, and PS_168_ISSUE_READY. Stop before the commencement task.

### Validation and evidence

Require complete pagination with no unvisited page, an exact issue-body readback, no unresolved placeholder, correct native relationships, no closing PR, no implementation marker, and unchanged repository bytes and refs.

### Stop and escalation conditions

Stop if the issue is closed or superseded without proved terminal evidence, another implementation owns the slot, a requested relationship is not a real prerequisite, the current body conflicts with repository evidence, or an update would broaden the issue beyond its proved scope. Record the exact conflict and do not commence work.

### Exact output

One authenticated `PS_168_ISSUE_READY` record states `ISSUE_READY_UNCHANGED` or `ISSUE_READY_UPDATED` and contains the exact issue identity, body identity, relationship state, scope matrix, and proof that implementation did not commence.

### Complete when

The issue is open, self-contained, current, and ready for its separate commencement leaf; exact readback proves its body and real relationships; no implementation or repository mutation occurred.

## Task 2 — commence PS PR-body identity synchronization

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 1 completion record

~~~markdown
# Task 1 completion record

{{TASK_001_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 1 | `FS` | PS #168 is open, self-contained, current, and ready; no implementation has commenced and the one-at-a-time slot is free. |

### Objective

Explicitly commence only the implementation governed by implement PS PR-body identity synchronization. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

- Preserve issue #168's exact durability objective: derive every PR-body identity from the checked-out head, compute the canonical policy-contract digest with `validatorIdentity` excluded by the validator's exact algorithm, and emit one deterministic Markdown block for the current head and tree.
- Detect a stale or partial identity block before automated review. Update all coupled generator, path-verifier, build-step, contract, and validator facts as one unit, or fail without changing the PR body.
- Authenticate every PR-body update through an exact readback. Never store credentials, API responses, or unrelated PR text in tracked artifacts.
- Do not change the generator algorithm or exact-path algorithm delivered by PS PR #167. Use a finding-specific decision to select the smallest repository-owned command, validation entry point, fixtures, and documentation paths.
- Require deterministic output, stale-block negative controls, partial-block negative controls, hostile or malformed value controls, exact head/tree binding, a no-change control, repository Markdown validation, and successful current-head CI.
- After the PS handoff, compare applicability in Terraform. Preserve byte-identical common CI behavior except proved repository-specific names and path roles.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 3 — implement PS PR-body identity synchronization

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 2 completion record

~~~markdown
# Task 2 completion record

{{TASK_002_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 2 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS PR-body identity synchronization without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

- Preserve issue #168's exact durability objective: derive every PR-body identity from the checked-out head, compute the canonical policy-contract digest with `validatorIdentity` excluded by the validator's exact algorithm, and emit one deterministic Markdown block for the current head and tree.
- Detect a stale or partial identity block before automated review. Update all coupled generator, path-verifier, build-step, contract, and validator facts as one unit, or fail without changing the PR body.
- Authenticate every PR-body update through an exact readback. Never store credentials, API responses, or unrelated PR text in tracked artifacts.
- Do not change the generator algorithm or exact-path algorithm delivered by PS PR #167. Use a finding-specific decision to select the smallest repository-owned command, validation entry point, fixtures, and documentation paths.
- Require deterministic output, stale-block negative controls, partial-block negative controls, hostile or malformed value controls, exact head/tree binding, a no-change control, repository Markdown validation, and successful current-head CI.
- After the PS handoff, compare applicability in Terraform. Preserve byte-identical common CI behavior except proved repository-specific names and path roles.

### Procedure

1. Work from the exact commenced baseline in an isolated branch or worktree.
2. Implement the complete task-local contract and preserve every stated scope boundary.
3. Run all task-specific local validation, commit the candidate, and record the commit, tree, paths, blobs, runtimes, exit codes, and results. Do not push or update a PR.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 4 — create or update the candidate PR for PS PR-body identity synchronization

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |
| Task 4 PR URL | `{{TASK_004_PR_URL}}` | Produced by this task; replace {{TASK_004_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 3 completion record

~~~markdown
# Task 3 completion record

{{TASK_003_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 3 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS PR-body identity synchronization; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

- Preserve issue #168's exact durability objective: derive every PR-body identity from the checked-out head, compute the canonical policy-contract digest with `validatorIdentity` excluded by the validator's exact algorithm, and emit one deterministic Markdown block for the current head and tree.
- Detect a stale or partial identity block before automated review. Update all coupled generator, path-verifier, build-step, contract, and validator facts as one unit, or fail without changing the PR body.
- Authenticate every PR-body update through an exact readback. Never store credentials, API responses, or unrelated PR text in tracked artifacts.
- Do not change the generator algorithm or exact-path algorithm delivered by PS PR #167. Use a finding-specific decision to select the smallest repository-owned command, validation entry point, fixtures, and documentation paths.
- Require deterministic output, stale-block negative controls, partial-block negative controls, hostile or malformed value controls, exact head/tree binding, a no-change control, repository Markdown validation, and successful current-head CI.
- After the PS handoff, compare applicability in Terraform. Preserve byte-identical common CI behavior except proved repository-specific names and path roles.

### Procedure

1. Re-query the governing issue, remote branch, base branch, active implementation slot, and required checks.
2. Push the committed candidate safely. Create or update only one focused PR that links the issue and accurately describes the complete diff, evidence, security effects, intentional differences, rollback, and deferred-work disposition.
3. Read the PR back, record its base, head/tree, commits, paths and blobs, body edit state, and issue links. Require all initial checks to pass. Stop before review or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #168 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #168 candidate PR exists with complete initial validation and an accurate PR body.

## Task 5 — run the Codex review loop on the PS #168 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |
| Task 4 PR URL | `{{TASK_004_PR_URL}}` | Produced by Task 4; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 4 completion record

~~~markdown
# Task 4 completion record

{{TASK_004_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 4 | `FS` | The PS #168 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the PS #168 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below against the PS #168 PR. Do not merge. If the loop changes bytes, rerun the Task 4 validation and continue on the new head.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 6 — run the independent final quality check on the PS #168 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |
| Task 4 PR URL | `{{TASK_004_PR_URL}}` | Produced by Task 4; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 5 completion record

~~~markdown
# Task 5 completion record

{{TASK_005_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 5 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #168 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below against the PS #168 PR. Require full issue-contract, workflow-topology, policy-case, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 5 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 5.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 5.

## Task 7 — merge the PS #168 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |
| Task 4 PR URL | `{{TASK_004_PR_URL}}` | Produced by Task 4; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 6 completion record

~~~markdown
# Task 6 completion record

{{TASK_006_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 6 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 8 — publish the landed handoff for the PS #168 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |
| Task 4 PR URL | `{{TASK_004_PR_URL}}` | Produced by Task 4; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 7 completion record

~~~markdown
# Task 7 completion record

{{TASK_007_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 7 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #168 is closed and the final landed policy contract is ready for Task 17.

### Complete when

PS #168 is closed and the final landed policy contract is ready for Task 17.

## Task 9 — compare the PR-body identity synchronization cycle in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 8 completion record

~~~markdown
# Task 8 completion record

{{TASK_008_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 8 | `FS` | Use the final landed PS #168 commit and policy contract. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 9 | Pin all Task 8 workflow, policy, contract, case, and validation blobs. |
| Task 9 | Complete the full foundation catalog and workflow-topology table in Terraform. |
| Task 9 | Apply all blockers from Task 8's required topology and policy. |
| Task 13 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 9 | Update PS #168, PS #159, and Terraform #31 with full identities and validation. |
| Task 9 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 9 | Map files by role, not only by filename. |
| Task 9 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 12 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 10 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 11 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 13 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 9 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 10 — create or update the focused repair issue for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 10 issue URL | `{{TASK_010_ISSUE_URL}}` | Produced by this task; replace {{TASK_010_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 9 completion record

~~~markdown
# Task 9 completion record

{{TASK_009_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 9 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 9's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 11 — commence the focused repair for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 10 issue URL | `{{TASK_010_ISSUE_URL}}` | Produced by Task 10; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 10 completion record

~~~markdown
# Task 10 completion record

{{TASK_010_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 10 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 10 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 12 — implement the focused repair for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 10 issue URL | `{{TASK_010_ISSUE_URL}}` | Produced by Task 10; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 11 completion record

~~~markdown
# Task 11 completion record

{{TASK_011_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 11 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 11 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 13 — create or update the focused repair PR for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 10 issue URL | `{{TASK_010_ISSUE_URL}}` | Produced by Task 10; resolve globally before execution |
| Task 13 PR URL | `{{TASK_013_PR_URL}}` | Produced by this task; replace {{TASK_013_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 12 completion record

~~~markdown
# Task 12 completion record

{{TASK_012_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 12 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 12 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 14 — run the Codex review loop on the focused repair PR for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 13 PR URL | `{{TASK_013_PR_URL}}` | Produced by Task 13; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 13 completion record

~~~markdown
# Task 13 completion record

{{TASK_013_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 13 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 13 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 13's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 14 for the new head before Task 15. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 15 — run the independent final quality check on the focused repair PR for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 13 PR URL | `{{TASK_013_PR_URL}}` | Produced by Task 13; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 14 completion record

~~~markdown
# Task 14 completion record

{{TASK_014_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 14 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 14 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 13's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 14 for the new head, and then repeat Task 15 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 16 — merge the focused repair PR for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 13 PR URL | `{{TASK_013_PR_URL}}` | Produced by Task 13; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 15 completion record

~~~markdown
# Task 15 completion record

{{TASK_015_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 15 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 15 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 17 — publish the landed repair handoff for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 13 PR URL | `{{TASK_013_PR_URL}}` | Produced by Task 13; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 16 completion record

~~~markdown
# Task 16 completion record

{{TASK_016_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 16 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 16 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 18 — reverse-compare the PR-body identity synchronization cycle in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 17 completion record

~~~markdown
# Task 17 completion record

{{TASK_017_COMPLETION_RECORD}}
~~~

#### Task 8 completion record

~~~markdown
# Task 8 completion record

{{TASK_008_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 17 | `FS` | Use only the final landed commits from Tasks 8 and 17. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 18 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 18 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 19 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 20 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 22 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 27 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 21 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 28 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 28 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 19 — create or update the focused repair issue for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 19 issue URL | `{{TASK_019_ISSUE_URL}}` | Produced by this task; replace {{TASK_019_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 18 completion record

~~~markdown
# Task 18 completion record

{{TASK_018_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 18 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 18's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 20 — commence the focused repair for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 19 issue URL | `{{TASK_019_ISSUE_URL}}` | Produced by Task 19; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 19 completion record

~~~markdown
# Task 19 completion record

{{TASK_019_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 19 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 19 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 21 — implement the focused repair for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 19 issue URL | `{{TASK_019_ISSUE_URL}}` | Produced by Task 19; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 20 completion record

~~~markdown
# Task 20 completion record

{{TASK_020_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 20 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 20 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 22 — create or update the focused repair PR for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 19 issue URL | `{{TASK_019_ISSUE_URL}}` | Produced by Task 19; resolve globally before execution |
| Task 22 PR URL | `{{TASK_022_PR_URL}}` | Produced by this task; replace {{TASK_022_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 21 completion record

~~~markdown
# Task 21 completion record

{{TASK_021_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 21 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 21 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 23 — run the Codex review loop on the focused repair PR for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 22 PR URL | `{{TASK_022_PR_URL}}` | Produced by Task 22; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 22 completion record

~~~markdown
# Task 22 completion record

{{TASK_022_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 22 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 22 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 22's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 23 for the new head before Task 24. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 24 — run the independent final quality check on the focused repair PR for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 22 PR URL | `{{TASK_022_PR_URL}}` | Produced by Task 22; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 23 completion record

~~~markdown
# Task 23 completion record

{{TASK_023_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 23 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 23 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 22's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 23 for the new head, and then repeat Task 24 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 25 — merge the focused repair PR for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 22 PR URL | `{{TASK_022_PR_URL}}` | Produced by Task 22; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 24 completion record

~~~markdown
# Task 24 completion record

{{TASK_024_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 24 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 24 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 26 — publish the landed repair handoff for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 22 PR URL | `{{TASK_022_PR_URL}}` | Produced by Task 22; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 25 completion record

~~~markdown
# Task 25 completion record

{{TASK_025_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 25 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 25 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 27 — recheck the reciprocal result for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 18 completion record

~~~markdown
# Task 18 completion record

{{TASK_018_COMPLETION_RECORD}}
~~~

#### Task 26 completion record

~~~markdown
# Task 26 completion record

{{TASK_026_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 18 | `FS` | Use the original pinned comparison and branch result. |
| Task 26 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 12–26 as a new numbered-range instance. After its new handoff, repeat Task 27.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 28 — publish the fixed-point closure for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 27 completion record

~~~markdown
# Task 27 completion record

{{TASK_027_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 27 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 27 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 29 — reverse-compare cycle 2 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

#### Completed PS #161 landed handoff

~~~markdown
# Completed PS #161 landed handoff

Issue comment `5334317038`; landed commit `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`; tree `215fc660c63e6c59f38f865faa3a441452c36605`; exact handoff SHA-256 `ec242de49873c0e7c1441d49067cf5d7a2270506e78ece9d70eba2343a4574ef`.
~~~

#### Completed Terraform #34 landed handoff

~~~markdown
# Completed Terraform #34 landed handoff

Issue comment `5336853959`; landed commit `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`; exact handoff SHA-256 `7961866368e93ba37a20c688f2dc849728290532717127acd0b7adba1ba33e5f`.
~~~

Task 28 supplies the completed PS #168 fixed-point record. Keep that later capability separate from the immutable PS #161 and Terraform #34 inputs compared in this task.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 28 | `FS` | The PS #168 reciprocal cycle is closed, and its final identities are recorded separately. |
| Completed PS #161 landed handoff | `FS` | Use only landed commit `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`, and comment `5334317038` for the PS cycle-2 input. |
| Completed Terraform #34 landed handoff | `FS` | Use only landed commit `fe28313e118497e2567bf306b74ec596dd10eb64`, tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`, and comment `5336853959` for the Terraform cycle-2 input. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 29 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 29 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 30 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 31 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 33 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 56 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 32 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 57 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 57 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Current blocker reconciliation

Current authenticated evidence identifies three distinct PS blockers tracked by #169, #170, and #171. A `REPAIR_REQUIRED` result must name all three, preserve their separate scopes, and order #169 first. Emit `NO_REPAIR` only if fresh landed-byte evidence proves every requirement in all three issues already satisfied and the issues have a defensible terminal disposition. If the branch differs from the current evidence, stop after publishing the comparison and run a new ordering decision before Task 30.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 30 — validate and update PS #169 for BSD identity dispatch and candidate-publication binding

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally with the exact value from its named source before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by this task |
| Issue URL | `https://github.com/franklesniak/PSStyleGuide/issues/169` | Verified existing open issue; re-query before execution |
| Planning-baseline body | 6,843 UTF-8 bytes; SHA-256 `f10c8d7e4dc6b0a7938f8e4f17bf193c7deada650fcf8d2824323375f1e903f6` | Authenticated read at the verified-state timestamp; current GitHub state controls |

### Record inputs

#### Task 29 completion record

~~~markdown
# Task 29 completion record

{{TASK_029_COMPLETION_RECORD}}
~~~

The issue currently has zero comments, body edits, labels, assignees, milestones, dependencies, parent or sub-issues, linked branches, and closing PRs. Re-query every connection and relationship with complete pagination before any write.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 29 | `FS` | Use its current pinned comparison and exact target-issue disposition. |

### Objective

Validate the existing issue as the complete, self-contained authority for BSD identity dispatch and candidate-publication binding. Update only missing or stale issue metadata that current repository and GitHub evidence proves necessary. Do not commence implementation, edit a repository byte, create a branch or PR, post an implementation comment, merge, close the issue, or change a repository setting.

### Execution controls

1. Use authenticated structured GitHub reads and paginate the issue body edits, comments, timeline, parent, sub-issues, blocked-by, blocking, tracked issues, linked branches, and closing references.
2. Re-query both repository `main` refs, all open implementation issues and PRs, and the one-at-a-time implementation slot.
3. Preserve the existing issue body unless a specific statement is false, incomplete, or no longer self-contained. Make one exact issue-body update only when evidence requires it.
4. Add a native parent or dependency only when it is a real completion prerequisite. A cross-reference, tracker membership, shared file, or desired order is not a blocking edge.
5. Keep issue update and implementation commencement separate. This task must not create a feature branch, edit tracked bytes, push, open a PR, or post `IMPLEMENTATION COMMENCED`.
6. Apply the issue-specific scope below. Do not merge this issue with another issue's implementation authority.
7. Read back every authorized change and compare its exact body, relationships, state, and timestamps. Preserve failure truth and stop after this task.

### Issue-specific scope

- Keep the Windows handle identity implementation. Select GNU `stat -Lc` only on Linux; select BSD `stat -f` on macOS and FreeBSD; reject an unknown non-Windows platform. Preserve exact link-count, device, inode, and hard-link refusal.
- Keep both post-publication final identity reads and every byte and serialization check. Require the stable final identity to equal the captured candidate identity. Report a post-publication mismatch as `ReplacementStateUncertain`; do not claim rollback.
- Limit the focused repair to `.github/workflows/Generate-StyleGuideArtifacts.ps1`, `.github/workflows/Validate-WorkflowPolicy.mjs`, and `.github/workflows/build.yml`. Do not change guides, generated artifacts, package files, lockfiles, action pins or inputs, permissions, triggers, job topology, artifact roles, credential roles, or settings.
- Pin and mutation-test platform dispatch and final-to-candidate equality. Run both PowerShell parsers, PSScriptAnalyzer with zero Warning/Error findings, GNU/BSD/unknown-platform controls, actual `File.Replace` and `File.Move` identity controls, the same-byte different-identity failure, policy preflight and catalog, and two generator passes on Windows PowerShell 5.1, Windows PowerShell 7, and native ext4 PowerShell 7.
- Start only from Terraform PR #35's landed commit, tree, blobs, review result, and the Task 29 ordered comparison. Never copy from its deleted or moving feature branch.

### Procedure

1. Capture the complete current issue and repository baseline.
2. Build a requirement-to-body matrix for every scope, exclusion, validation, evidence, relationship, terminal, and reopen condition above.
3. If the body is already complete and current, record `ISSUE_READY_UNCHANGED`. If it is not, validate the defect, use a finding-specific decision process, update only the issue body or real relationship, read it back, and record `ISSUE_READY_UPDATED`.
4. Confirm that no implementation commenced and that the implementation slot remains free.
5. Return the issue URL, node ID, state, body bytes and SHA-256, relationship inventory, cross-references, exact mutation or no-change result, and PS_169_ISSUE_READY. Stop before the commencement task.

### Validation and evidence

Require complete pagination with no unvisited page, an exact issue-body readback, no unresolved placeholder, correct native relationships, no closing PR, no implementation marker, and unchanged repository bytes and refs.

### Stop and escalation conditions

Stop if the issue is closed or superseded without proved terminal evidence, another implementation owns the slot, a requested relationship is not a real prerequisite, the current body conflicts with repository evidence, or an update would broaden the issue beyond its proved scope. Record the exact conflict and do not commence work.

### Exact output

One authenticated `PS_169_ISSUE_READY` record states `ISSUE_READY_UNCHANGED` or `ISSUE_READY_UPDATED` and contains the exact issue identity, body identity, relationship state, scope matrix, and proof that implementation did not commence.

### Complete when

The issue is open, self-contained, current, and ready for its separate commencement leaf; exact readback proves its body and real relationships; no implementation or repository mutation occurred.

## Task 31 — commence the PS #169 generator identity repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #169 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/169}` | Verified existing PS #169 issue; re-query before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 30 completion record

~~~markdown
# Task 30 completion record

{{TASK_030_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 30 | `FS` | A focused issue exists, and the current issue-ready branch is active. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Issue-specific scope

- Keep the Windows handle identity implementation. Select GNU `stat -Lc` only on Linux; select BSD `stat -f` on macOS and FreeBSD; reject an unknown non-Windows platform. Preserve exact link-count, device, inode, and hard-link refusal.
- Keep both post-publication final identity reads and every byte and serialization check. Require the stable final identity to equal the captured candidate identity. Report a post-publication mismatch as `ReplacementStateUncertain`; do not claim rollback.
- Limit the focused repair to `.github/workflows/Generate-StyleGuideArtifacts.ps1`, `.github/workflows/Validate-WorkflowPolicy.mjs`, and `.github/workflows/build.yml`. Do not change guides, generated artifacts, package files, lockfiles, action pins or inputs, permissions, triggers, job topology, artifact roles, credential roles, or settings.
- Pin and mutation-test platform dispatch and final-to-candidate equality. Run both PowerShell parsers, PSScriptAnalyzer with zero Warning/Error findings, GNU/BSD/unknown-platform controls, actual `File.Replace` and `File.Move` identity controls, the same-byte different-identity failure, policy preflight and catalog, and two generator passes on Windows PowerShell 5.1, Windows PowerShell 7, and native ext4 PowerShell 7.
- Start only from Terraform PR #35's landed commit, tree, blobs, review result, and the Task 29 ordered comparison. Never copy from its deleted or moving feature branch.

### Procedure

1. Require Task 30 to return the issue-ready record. Stop if it did not.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

## Task 32 — implement the PS #169 generator identity repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #169 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/169}` | Verified existing PS #169 issue; re-query before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 31 completion record

~~~markdown
# Task 31 completion record

{{TASK_031_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 31 | `FS` | The focused issue is commenced, and the current issue-ready branch is active. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Issue-specific scope

- Keep the Windows handle identity implementation. Select GNU `stat -Lc` only on Linux; select BSD `stat -f` on macOS and FreeBSD; reject an unknown non-Windows platform. Preserve exact link-count, device, inode, and hard-link refusal.
- Keep both post-publication final identity reads and every byte and serialization check. Require the stable final identity to equal the captured candidate identity. Report a post-publication mismatch as `ReplacementStateUncertain`; do not claim rollback.
- Limit the focused repair to `.github/workflows/Generate-StyleGuideArtifacts.ps1`, `.github/workflows/Validate-WorkflowPolicy.mjs`, and `.github/workflows/build.yml`. Do not change guides, generated artifacts, package files, lockfiles, action pins or inputs, permissions, triggers, job topology, artifact roles, credential roles, or settings.
- Pin and mutation-test platform dispatch and final-to-candidate equality. Run both PowerShell parsers, PSScriptAnalyzer with zero Warning/Error findings, GNU/BSD/unknown-platform controls, actual `File.Replace` and `File.Move` identity controls, the same-byte different-identity failure, policy preflight and catalog, and two generator passes on Windows PowerShell 5.1, Windows PowerShell 7, and native ext4 PowerShell 7.
- Start only from Terraform PR #35's landed commit, tree, blobs, review result, and the Task 29 ordered comparison. Never copy from its deleted or moving feature branch.

### Procedure

1. If Task 31 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

## Task 33 — create or update the PS #169 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #169 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/169}` | Verified existing PS #169 issue; re-query before execution |
| Task 33 PR URL | `{{TASK_033_PR_URL}}` | Produced by this task; replace {{TASK_033_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 32 completion record

~~~markdown
# Task 32 completion record

{{TASK_032_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 32 | `FS` | Use the committed repair candidate, and the current issue-ready branch is active. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Issue-specific scope

- Keep the Windows handle identity implementation. Select GNU `stat -Lc` only on Linux; select BSD `stat -f` on macOS and FreeBSD; reject an unknown non-Windows platform. Preserve exact link-count, device, inode, and hard-link refusal.
- Keep both post-publication final identity reads and every byte and serialization check. Require the stable final identity to equal the captured candidate identity. Report a post-publication mismatch as `ReplacementStateUncertain`; do not claim rollback.
- Limit the focused repair to `.github/workflows/Generate-StyleGuideArtifacts.ps1`, `.github/workflows/Validate-WorkflowPolicy.mjs`, and `.github/workflows/build.yml`. Do not change guides, generated artifacts, package files, lockfiles, action pins or inputs, permissions, triggers, job topology, artifact roles, credential roles, or settings.
- Pin and mutation-test platform dispatch and final-to-candidate equality. Run both PowerShell parsers, PSScriptAnalyzer with zero Warning/Error findings, GNU/BSD/unknown-platform controls, actual `File.Replace` and `File.Move` identity controls, the same-byte different-identity failure, policy preflight and catalog, and two generator passes on Windows PowerShell 5.1, Windows PowerShell 7, and native ext4 PowerShell 7.
- Start only from Terraform PR #35's landed commit, tree, blobs, review result, and the Task 29 ordered comparison. Never copy from its deleted or moving feature branch.

### Procedure

1. If Task 32 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

## Task 34 — run the Codex review loop on the PS #169 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 33 PR URL | `{{TASK_033_PR_URL}}` | Produced by Task 33; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 33 completion record

~~~markdown
# Task 33 completion record

{{TASK_033_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 33 | `FS` | The focused repair PR is review-ready, and the current issue-ready branch is active. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 33 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 33's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 34 for the new head before Task 35. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

## Task 35 — run the independent final quality check on the PS #169 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 33 PR URL | `{{TASK_033_PR_URL}}` | Produced by Task 33; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 34 completion record

~~~markdown
# Task 34 completion record

{{TASK_034_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 34 | `FS` | A terminal-clean record applies to the current repair head/tree, and the current issue-ready branch is active. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 34 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 33's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 34 for the new head, and then repeat Task 35 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

## Task 36 — merge the PS #169 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 33 PR URL | `{{TASK_033_PR_URL}}` | Produced by Task 33; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 35 completion record

~~~markdown
# Task 35 completion record

{{TASK_035_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 35 | `FS` | Both review gates apply to the same current head/tree, and the current issue-ready branch is active. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 35 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

## Task 37 — publish the landed handoff for PS #169

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 33 PR URL | `{{TASK_033_PR_URL}}` | Produced by Task 33; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 36 completion record

~~~markdown
# Task 36 completion record

{{TASK_036_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 36 | `FS` | The repair PR is merged with an actual landed identity, and the current issue-ready branch is active. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 36 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

## Task 38 — compare landed PS #169 with Terraform and authorize PS #170

> **Execution class: Coding agent executable.** This is a read-only comparison plus evidence-publication leaf.

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally from its named predecessor before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repositories | `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by this task |
| Completed PS issue | `https://github.com/franklesniak/PSStyleGuide/issues/169` | Require closed-by-merged-PR evidence |
| Next PS issue | `https://github.com/franklesniak/PSStyleGuide/issues/170` | Verified open issue; this task does not edit it |

### Record inputs

#### Task 29 completion record

~~~markdown
# Task 29 completion record

{{TASK_029_COMPLETION_RECORD}}
~~~

#### Task 37 completion record

~~~markdown
# Task 37 completion record

{{TASK_037_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 29 | `FS` | Its pinned Terraform PR #35 and PS comparison evidence remains current for the property sequence. |
| Task 37 | `FS` | PS #169 is closed by a reviewed merged PR and has an authenticated landed handoff. |

### Objective

Compare the landed PS #169 result with the immutable Terraform PR #35 implementation and current canonical refs. Prove that the completed issue introduced no reciprocal regression. Publish the pinned comparison and authorize PS #170 only when it is the exact next remaining PS blocker. Do not update or commence #170, edit repository bytes, create a PR, merge, or change settings.

### Execution controls

1. Fetch and pin both current `origin/main` refs, the completed PS PR's reviewed and landed identities, Terraform PR #35 merge `fe28313e118497e2567bf306b74ec596dd10eb64` and tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`, and every compared Git blob.
2. Read source bytes only from recorded landed commits. Do not copy or compare from a moving feature branch.
3. Compare the complete common foundation catalog and the property scope below. Classify each applicable row exactly once as `same`, `intentional difference`, or `blocker`.
4. Prove every intentional difference with both behaviors, repository need, equal security and failure strength, owner, and review or expiry condition.
5. Re-query issue #170, its body edits, comments, dependencies, cross-references, linked branches, closing references, and implementation markers with complete pagination.
6. The only permitted public write is the exact comparison evidence comment on the applicable existing trackers or issues. Do not edit an issue body or relationship in this task.
7. Stop if a new Terraform repair is required, #170 is not the next remaining blocker, a row changes direction twice, or any landed identity is unavailable. Use a new decision before changing the planned order.

### Property scope

Compare GNU/BSD/unknown-platform dispatch, link-count/device/inode semantics, hard-link refusal, final identity stability, final-to-candidate equality, post-publication uncertainty truth, policy mutations, runtime matrices, and every coupled version or identity. Require #169 exact scope to be closed before #170 starts.

### Procedure

1. Validate both predecessor records and re-query all current identities and review/check evidence.
2. Build the complete role-to-path and property matrix from the immutable landed commits. Include repository-specific substitutions explicitly.
3. Verify that every #169 requirement is present in the landed PS bytes and that the corresponding Terraform behavior is equal or a proved intentional difference.
4. Verify that the only planned remaining blocker is the exact #170 scope. Do not treat an issue cross-reference as implementation or completion evidence.
5. Publish one authenticated comparison record that states `REPAIR_REQUIRED` with target PS #170, exact pinned inputs, affected roles and paths, required validation, and expected terminal state.
6. Read back the comment, verify its exact body identity, and stop without starting Task 39.

### Validation and evidence

Require complete GitHub pagination, exact commit/tree/blob identities, each applicable catalog row exactly once, no unexplained difference, successful landed checks, closed #169 evidence, open and uncommenced #170 evidence, and an authenticated comparison-comment readback.

### Stop and escalation conditions

Stop if the comparison can emit `FIXED_POINT_READY`, targets Terraform, identifies a blocker outside #170, finds #169 incomplete, or finds changed identities or unfinished review. Record the exact result and run a new ordering decision; do not force the expected branch.

### Exact output

One pinned comparison record emits `REPAIR_REQUIRED` for PS #170 and proves that PS #169 is landed, reviewed, reciprocally safe, and no longer a blocker.

### Complete when

Authenticated readback proves the comparison record is complete and exact, #169 is reciprocally closed, and PS #170 is the only authorized next issue; no implementation has commenced.

## Task 39 — validate and update PS #170 for culture-invariant rationale anchors

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally with the exact value from its named source before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by this task |
| Issue URL | `https://github.com/franklesniak/PSStyleGuide/issues/170` | Verified existing open issue; re-query before execution |
| Planning-baseline body | 4,333 UTF-8 bytes; SHA-256 `59771ad2e7d87d19f4b9a3bf8e8343a7e78d6ef5b51ac9ca00254e85d99dd547` | Authenticated read at the verified-state timestamp; current GitHub state controls |

### Record inputs

#### Task 38 completion record

~~~markdown
# Task 38 completion record

{{TASK_038_COMPLETION_RECORD}}
~~~

The issue currently has zero comments, body edits, labels, assignees, milestones, dependencies, parent or sub-issues, linked branches, and closing PRs. Re-query every connection and relationship with complete pagination before any write.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 38 | `FS` | Use its current pinned comparison and exact target-issue disposition. |

### Objective

Validate the existing issue as the complete, self-contained authority for culture-invariant rationale anchors. Update only missing or stale issue metadata that current repository and GitHub evidence proves necessary. Do not commence implementation, edit a repository byte, create a branch or PR, post an implementation comment, merge, close the issue, or change a repository setting.

### Execution controls

1. Use authenticated structured GitHub reads and paginate the issue body edits, comments, timeline, parent, sub-issues, blocked-by, blocking, tracked issues, linked branches, and closing references.
2. Re-query both repository `main` refs, all open implementation issues and PRs, and the one-at-a-time implementation slot.
3. Preserve the existing issue body unless a specific statement is false, incomplete, or no longer self-contained. Make one exact issue-body update only when evidence requires it.
4. Add a native parent or dependency only when it is a real completion prerequisite. A cross-reference, tracker membership, shared file, or desired order is not a blocking edge.
5. Keep issue update and implementation commencement separate. This task must not create a feature branch, edit tracked bytes, push, open a PR, or post `IMPLEMENTATION COMMENCED`.
6. Apply the issue-specific scope below. Do not merge this issue with another issue's implementation authority.
7. Read back every authorized change and compare its exact body, relationships, state, and timestamps. Preserve failure truth and stop after this task.

### Issue-specific scope

- Replace both affected rationale-anchor conversions with `.ToLowerInvariant()` or an equivalently narrow invariant-culture operation. Do not change process-wide culture, ASCII filtering, or dash normalization without a separately measured defect and decision.
- Bind both conversions in the current PS machine-readable policy authority and case catalog, or prove an equally strong existing authority. Add independently reachable mutations that restore culture-sensitive casing at each conversion and fail their dedicated invariant-casing rule before a digest backstop.
- Use PS baseline commit `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`, and generator blob `e19080ef9108fcdec66b634037b891a94352723d` only to reproduce the original defect; re-read the canonical baseline produced by Task 38 before editing.
- Run both PowerShell parsers, PSScriptAnalyzer, extracted `en-US` and `tr-TR` `Identity`/`identity` controls for both helpers, policy preflight and every catalog case exactly once, the two-run three-host generator matrix, artifact byte equality and idempotence, generated-drift checks, exact scope, current-head CI, review, and landed handoff.
- Keep this issue separate from #169 and link the landed Terraform repair in the final handoff.

### Procedure

1. Capture the complete current issue and repository baseline.
2. Build a requirement-to-body matrix for every scope, exclusion, validation, evidence, relationship, terminal, and reopen condition above.
3. If the body is already complete and current, record `ISSUE_READY_UNCHANGED`. If it is not, validate the defect, use a finding-specific decision process, update only the issue body or real relationship, read it back, and record `ISSUE_READY_UPDATED`.
4. Confirm that no implementation commenced and that the implementation slot remains free.
5. Return the issue URL, node ID, state, body bytes and SHA-256, relationship inventory, cross-references, exact mutation or no-change result, and PS_170_ISSUE_READY. Stop before the commencement task.

### Validation and evidence

Require complete pagination with no unvisited page, an exact issue-body readback, no unresolved placeholder, correct native relationships, no closing PR, no implementation marker, and unchanged repository bytes and refs.

### Stop and escalation conditions

Stop if the issue is closed or superseded without proved terminal evidence, another implementation owns the slot, a requested relationship is not a real prerequisite, the current body conflicts with repository evidence, or an update would broaden the issue beyond its proved scope. Record the exact conflict and do not commence work.

### Exact output

One authenticated `PS_170_ISSUE_READY` record states `ISSUE_READY_UNCHANGED` or `ISSUE_READY_UPDATED` and contains the exact issue identity, body identity, relationship state, scope matrix, and proof that implementation did not commence.

### Complete when

The issue is open, self-contained, current, and ready for its separate commencement leaf; exact readback proves its body and real relationships; no implementation or repository mutation occurred.

## Task 40 — commence the PS #170 culture-invariant anchor repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #170 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/170}` | Verified existing PS #170 issue; re-query before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 39 completion record

~~~markdown
# Task 39 completion record

{{TASK_039_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 39 | `FS` | A focused issue exists, and the current issue-ready branch is active. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Issue-specific scope

- Replace both affected rationale-anchor conversions with `.ToLowerInvariant()` or an equivalently narrow invariant-culture operation. Do not change process-wide culture, ASCII filtering, or dash normalization without a separately measured defect and decision.
- Bind both conversions in the current PS machine-readable policy authority and case catalog, or prove an equally strong existing authority. Add independently reachable mutations that restore culture-sensitive casing at each conversion and fail their dedicated invariant-casing rule before a digest backstop.
- Use PS baseline commit `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`, and generator blob `e19080ef9108fcdec66b634037b891a94352723d` only to reproduce the original defect; re-read the canonical baseline produced by Task 38 before editing.
- Run both PowerShell parsers, PSScriptAnalyzer, extracted `en-US` and `tr-TR` `Identity`/`identity` controls for both helpers, policy preflight and every catalog case exactly once, the two-run three-host generator matrix, artifact byte equality and idempotence, generated-drift checks, exact scope, current-head CI, review, and landed handoff.
- Keep this issue separate from #169 and link the landed Terraform repair in the final handoff.

### Procedure

1. Require Task 39 to return the issue-ready record. Stop if it did not.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

## Task 41 — implement the PS #170 culture-invariant anchor repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #170 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/170}` | Verified existing PS #170 issue; re-query before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 40 completion record

~~~markdown
# Task 40 completion record

{{TASK_040_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 40 | `FS` | The focused issue is commenced, and the current issue-ready branch is active. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Issue-specific scope

- Replace both affected rationale-anchor conversions with `.ToLowerInvariant()` or an equivalently narrow invariant-culture operation. Do not change process-wide culture, ASCII filtering, or dash normalization without a separately measured defect and decision.
- Bind both conversions in the current PS machine-readable policy authority and case catalog, or prove an equally strong existing authority. Add independently reachable mutations that restore culture-sensitive casing at each conversion and fail their dedicated invariant-casing rule before a digest backstop.
- Use PS baseline commit `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`, and generator blob `e19080ef9108fcdec66b634037b891a94352723d` only to reproduce the original defect; re-read the canonical baseline produced by Task 38 before editing.
- Run both PowerShell parsers, PSScriptAnalyzer, extracted `en-US` and `tr-TR` `Identity`/`identity` controls for both helpers, policy preflight and every catalog case exactly once, the two-run three-host generator matrix, artifact byte equality and idempotence, generated-drift checks, exact scope, current-head CI, review, and landed handoff.
- Keep this issue separate from #169 and link the landed Terraform repair in the final handoff.

### Procedure

1. If Task 40 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

## Task 42 — create or update the PS #170 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #170 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/170}` | Verified existing PS #170 issue; re-query before execution |
| Task 42 PR URL | `{{TASK_042_PR_URL}}` | Produced by this task; replace {{TASK_042_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 41 completion record

~~~markdown
# Task 41 completion record

{{TASK_041_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 41 | `FS` | Use the committed repair candidate, and the current issue-ready branch is active. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Issue-specific scope

- Replace both affected rationale-anchor conversions with `.ToLowerInvariant()` or an equivalently narrow invariant-culture operation. Do not change process-wide culture, ASCII filtering, or dash normalization without a separately measured defect and decision.
- Bind both conversions in the current PS machine-readable policy authority and case catalog, or prove an equally strong existing authority. Add independently reachable mutations that restore culture-sensitive casing at each conversion and fail their dedicated invariant-casing rule before a digest backstop.
- Use PS baseline commit `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`, and generator blob `e19080ef9108fcdec66b634037b891a94352723d` only to reproduce the original defect; re-read the canonical baseline produced by Task 38 before editing.
- Run both PowerShell parsers, PSScriptAnalyzer, extracted `en-US` and `tr-TR` `Identity`/`identity` controls for both helpers, policy preflight and every catalog case exactly once, the two-run three-host generator matrix, artifact byte equality and idempotence, generated-drift checks, exact scope, current-head CI, review, and landed handoff.
- Keep this issue separate from #169 and link the landed Terraform repair in the final handoff.

### Procedure

1. If Task 41 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

## Task 43 — run the Codex review loop on the PS #170 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 42 PR URL | `{{TASK_042_PR_URL}}` | Produced by Task 42; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 42 completion record

~~~markdown
# Task 42 completion record

{{TASK_042_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 42 | `FS` | The focused repair PR is review-ready, and the current issue-ready branch is active. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 42 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 42's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 43 for the new head before Task 44. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

## Task 44 — run the independent final quality check on the PS #170 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 42 PR URL | `{{TASK_042_PR_URL}}` | Produced by Task 42; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 43 completion record

~~~markdown
# Task 43 completion record

{{TASK_043_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 43 | `FS` | A terminal-clean record applies to the current repair head/tree, and the current issue-ready branch is active. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 43 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 42's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 43 for the new head, and then repeat Task 44 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

## Task 45 — merge the PS #170 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 42 PR URL | `{{TASK_042_PR_URL}}` | Produced by Task 42; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 44 completion record

~~~markdown
# Task 44 completion record

{{TASK_044_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 44 | `FS` | Both review gates apply to the same current head/tree, and the current issue-ready branch is active. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 44 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

## Task 46 — publish the landed handoff for PS #170

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 42 PR URL | `{{TASK_042_PR_URL}}` | Produced by Task 42; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 45 completion record

~~~markdown
# Task 45 completion record

{{TASK_045_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 45 | `FS` | The repair PR is merged with an actual landed identity, and the current issue-ready branch is active. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 45 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

## Task 47 — compare landed PS #170 with Terraform and authorize PS #171

> **Execution class: Coding agent executable.** This is a read-only comparison plus evidence-publication leaf.

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally from its named predecessor before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repositories | `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by this task |
| Completed PS issue | `https://github.com/franklesniak/PSStyleGuide/issues/170` | Require closed-by-merged-PR evidence |
| Next PS issue | `https://github.com/franklesniak/PSStyleGuide/issues/171` | Verified open issue; this task does not edit it |

### Record inputs

#### Task 38 completion record

~~~markdown
# Task 38 completion record

{{TASK_038_COMPLETION_RECORD}}
~~~

#### Task 46 completion record

~~~markdown
# Task 46 completion record

{{TASK_046_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 38 | `FS` | Its pinned Terraform PR #35 and PS comparison evidence remains current for the property sequence. |
| Task 46 | `FS` | PS #170 is closed by a reviewed merged PR and has an authenticated landed handoff. |

### Objective

Compare the landed PS #170 result with the immutable Terraform PR #35 implementation and current canonical refs. Prove that the completed issue introduced no reciprocal regression. Publish the pinned comparison and authorize PS #171 only when it is the exact next remaining PS blocker. Do not update or commence #171, edit repository bytes, create a PR, merge, or change settings.

### Execution controls

1. Fetch and pin both current `origin/main` refs, the completed PS PR's reviewed and landed identities, Terraform PR #35 merge `fe28313e118497e2567bf306b74ec596dd10eb64` and tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`, and every compared Git blob.
2. Read source bytes only from recorded landed commits. Do not copy or compare from a moving feature branch.
3. Compare the complete common foundation catalog and the property scope below. Classify each applicable row exactly once as `same`, `intentional difference`, or `blocker`.
4. Prove every intentional difference with both behaviors, repository need, equal security and failure strength, owner, and review or expiry condition.
5. Re-query issue #171, its body edits, comments, dependencies, cross-references, linked branches, closing references, and implementation markers with complete pagination.
6. The only permitted public write is the exact comparison evidence comment on the applicable existing trackers or issues. Do not edit an issue body or relationship in this task.
7. Stop if a new Terraform repair is required, #171 is not the next remaining blocker, a row changes direction twice, or any landed identity is unavailable. Use a new decision before changing the planned order.

### Property scope

Compare both invariant anchor conversions, ASCII filtering and dash normalization, policy authority, independent mutation fixtures, culture-control outputs, generator versions and identities, runtime matrices, generated-byte stability, and every intentional repository-specific difference. Require #170 exact scope to be closed before #171 starts.

### Procedure

1. Validate both predecessor records and re-query all current identities and review/check evidence.
2. Build the complete role-to-path and property matrix from the immutable landed commits. Include repository-specific substitutions explicitly.
3. Verify that every #170 requirement is present in the landed PS bytes and that the corresponding Terraform behavior is equal or a proved intentional difference.
4. Verify that the only planned remaining blocker is the exact #171 scope. Do not treat an issue cross-reference as implementation or completion evidence.
5. Publish one authenticated comparison record that states `REPAIR_REQUIRED` with target PS #171, exact pinned inputs, affected roles and paths, required validation, and expected terminal state.
6. Read back the comment, verify its exact body identity, and stop without starting Task 48.

### Validation and evidence

Require complete GitHub pagination, exact commit/tree/blob identities, each applicable catalog row exactly once, no unexplained difference, successful landed checks, closed #170 evidence, open and uncommenced #171 evidence, and an authenticated comparison-comment readback.

### Stop and escalation conditions

Stop if the comparison can emit `FIXED_POINT_READY`, targets Terraform, identifies a blocker outside #171, finds #170 incomplete, or finds changed identities or unfinished review. Record the exact result and run a new ordering decision; do not force the expected branch.

### Exact output

One pinned comparison record emits `REPAIR_REQUIRED` for PS #171 and proves that PS #170 is landed, reviewed, reciprocally safe, and no longer a blocker.

### Complete when

Authenticated readback proves the comparison record is complete and exact, #170 is reciprocally closed, and PS #171 is the only authorized next issue; no implementation has commenced.

## Task 48 — validate and update PS #171 for bounded field-specific generator diagnostics

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally with the exact value from its named source before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by this task |
| Issue URL | `https://github.com/franklesniak/PSStyleGuide/issues/171` | Verified existing open issue; re-query before execution |
| Planning-baseline body | 5,171 UTF-8 bytes; SHA-256 `6a2e10e574348a283b60334b4b95ec14d03af71d937799e862460aabdfa9c9c1` | Authenticated read at the verified-state timestamp; current GitHub state controls |

### Record inputs

#### Task 47 completion record

~~~markdown
# Task 47 completion record

{{TASK_047_COMPLETION_RECORD}}
~~~

The issue currently has zero comments, body edits, labels, assignees, milestones, dependencies, parent or sub-issues, linked branches, and closing PRs. Re-query every connection and relationship with complete pagination before any write.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 47 | `FS` | Use its current pinned comparison and exact target-issue disposition. |

### Objective

Validate the existing issue as the complete, self-contained authority for bounded field-specific generator diagnostics. Update only missing or stale issue metadata that current repository and GitHub evidence proves necessary. Do not commence implementation, edit a repository byte, create a branch or PR, post an implementation comment, merge, close the issue, or change a repository setting.

### Execution controls

1. Use authenticated structured GitHub reads and paginate the issue body edits, comments, timeline, parent, sub-issues, blocked-by, blocking, tracked issues, linked branches, and closing references.
2. Re-query both repository `main` refs, all open implementation issues and PRs, and the one-at-a-time implementation slot.
3. Preserve the existing issue body unless a specific statement is false, incomplete, or no longer self-contained. Make one exact issue-body update only when evidence requires it.
4. Add a native parent or dependency only when it is a real completion prerequisite. A cross-reference, tracker membership, shared file, or desired order is not a blocking edge.
5. Keep issue update and implementation commencement separate. This task must not create a feature branch, edit tracked bytes, push, open a PR, or post `IMPLEMENTATION COMMENCED`.
6. Apply the issue-specific scope below. Do not merge this issue with another issue's implementation authority.
7. Read back every authorized change and compare its exact body, relationships, state, and timestamps. Preserve failure truth and stop after this task.

### Issue-specific scope

- Evaluate result checks in fixed order: `NativeExit`, `Schema`, `GeneratorVersion`, `Overall`, `Phase`, `Category`, `NativeOutcome`, and `ResultExitCode`, when those fields remain in the landed schema. Accumulate every failure; do not stop after native exit or the first JSON field.
- Render null as `<null>`. Sanitize each actual value to printable ASCII, keep at most 64 sanitized characters, and add `...` only after truncation. Keep native process status separate from the generator result exit code.
- Preserve successful-result behavior, ordered artifact validation, and generated bytes. Bind the names and order, null marker, sanitization, bound, truncation marker, native/result distinction, accumulator, and deciding guard in the current PS policy authority with isolated negative cases and valid/hostile controls.
- Coordinate with open PS #162. Do not weaken its no-publication boundary. If actual values cannot safely appear in the current repository-code job, implement and document a stronger compatible channel that preserves named distinctions and fixed ordering without exposing checkout-controlled content.
- Re-read the canonical baseline produced by Task 47 and current #162 body before design. Run both parsers, PSScriptAnalyzer, policy preflight and all cases once, hostile and valid controls, the complete two-run three-host generator matrix, generated-drift and exact-scope checks, current-head CI, review, and landed handoff.

### Procedure

1. Capture the complete current issue and repository baseline.
2. Build a requirement-to-body matrix for every scope, exclusion, validation, evidence, relationship, terminal, and reopen condition above.
3. If the body is already complete and current, record `ISSUE_READY_UNCHANGED`. If it is not, validate the defect, use a finding-specific decision process, update only the issue body or real relationship, read it back, and record `ISSUE_READY_UPDATED`.
4. Confirm that no implementation commenced and that the implementation slot remains free.
5. Return the issue URL, node ID, state, body bytes and SHA-256, relationship inventory, cross-references, exact mutation or no-change result, and PS_171_ISSUE_READY. Stop before the commencement task.

### Validation and evidence

Require complete pagination with no unvisited page, an exact issue-body readback, no unresolved placeholder, correct native relationships, no closing PR, no implementation marker, and unchanged repository bytes and refs.

### Stop and escalation conditions

Stop if the issue is closed or superseded without proved terminal evidence, another implementation owns the slot, a requested relationship is not a real prerequisite, the current body conflicts with repository evidence, or an update would broaden the issue beyond its proved scope. Record the exact conflict and do not commence work.

### Exact output

One authenticated `PS_171_ISSUE_READY` record states `ISSUE_READY_UNCHANGED` or `ISSUE_READY_UPDATED` and contains the exact issue identity, body identity, relationship state, scope matrix, and proof that implementation did not commence.

### Complete when

The issue is open, self-contained, current, and ready for its separate commencement leaf; exact readback proves its body and real relationships; no implementation or repository mutation occurred.

## Task 49 — commence the PS #171 bounded diagnostic repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #171 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/171}` | Verified existing PS #171 issue; re-query before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 48 completion record

~~~markdown
# Task 48 completion record

{{TASK_048_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 48 | `FS` | A focused issue exists, and the current issue-ready branch is active. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Issue-specific scope

- Evaluate result checks in fixed order: `NativeExit`, `Schema`, `GeneratorVersion`, `Overall`, `Phase`, `Category`, `NativeOutcome`, and `ResultExitCode`, when those fields remain in the landed schema. Accumulate every failure; do not stop after native exit or the first JSON field.
- Render null as `<null>`. Sanitize each actual value to printable ASCII, keep at most 64 sanitized characters, and add `...` only after truncation. Keep native process status separate from the generator result exit code.
- Preserve successful-result behavior, ordered artifact validation, and generated bytes. Bind the names and order, null marker, sanitization, bound, truncation marker, native/result distinction, accumulator, and deciding guard in the current PS policy authority with isolated negative cases and valid/hostile controls.
- Coordinate with open PS #162. Do not weaken its no-publication boundary. If actual values cannot safely appear in the current repository-code job, implement and document a stronger compatible channel that preserves named distinctions and fixed ordering without exposing checkout-controlled content.
- Re-read the canonical baseline produced by Task 47 and current #162 body before design. Run both parsers, PSScriptAnalyzer, policy preflight and all cases once, hostile and valid controls, the complete two-run three-host generator matrix, generated-drift and exact-scope checks, current-head CI, review, and landed handoff.

### Procedure

1. Require Task 48 to return the issue-ready record. Stop if it did not.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

## Task 50 — implement the PS #171 bounded diagnostic repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #171 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/171}` | Verified existing PS #171 issue; re-query before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 49 completion record

~~~markdown
# Task 49 completion record

{{TASK_049_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 49 | `FS` | The focused issue is commenced, and the current issue-ready branch is active. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Issue-specific scope

- Evaluate result checks in fixed order: `NativeExit`, `Schema`, `GeneratorVersion`, `Overall`, `Phase`, `Category`, `NativeOutcome`, and `ResultExitCode`, when those fields remain in the landed schema. Accumulate every failure; do not stop after native exit or the first JSON field.
- Render null as `<null>`. Sanitize each actual value to printable ASCII, keep at most 64 sanitized characters, and add `...` only after truncation. Keep native process status separate from the generator result exit code.
- Preserve successful-result behavior, ordered artifact validation, and generated bytes. Bind the names and order, null marker, sanitization, bound, truncation marker, native/result distinction, accumulator, and deciding guard in the current PS policy authority with isolated negative cases and valid/hostile controls.
- Coordinate with open PS #162. Do not weaken its no-publication boundary. If actual values cannot safely appear in the current repository-code job, implement and document a stronger compatible channel that preserves named distinctions and fixed ordering without exposing checkout-controlled content.
- Re-read the canonical baseline produced by Task 47 and current #162 body before design. Run both parsers, PSScriptAnalyzer, policy preflight and all cases once, hostile and valid controls, the complete two-run three-host generator matrix, generated-drift and exact-scope checks, current-head CI, review, and landed handoff.

### Procedure

1. If Task 49 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

## Task 51 — create or update the PS #171 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #171 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/171}` | Verified existing PS #171 issue; re-query before execution |
| Task 51 PR URL | `{{TASK_051_PR_URL}}` | Produced by this task; replace {{TASK_051_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 50 completion record

~~~markdown
# Task 50 completion record

{{TASK_050_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 50 | `FS` | Use the committed repair candidate, and the current issue-ready branch is active. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Issue-specific scope

- Evaluate result checks in fixed order: `NativeExit`, `Schema`, `GeneratorVersion`, `Overall`, `Phase`, `Category`, `NativeOutcome`, and `ResultExitCode`, when those fields remain in the landed schema. Accumulate every failure; do not stop after native exit or the first JSON field.
- Render null as `<null>`. Sanitize each actual value to printable ASCII, keep at most 64 sanitized characters, and add `...` only after truncation. Keep native process status separate from the generator result exit code.
- Preserve successful-result behavior, ordered artifact validation, and generated bytes. Bind the names and order, null marker, sanitization, bound, truncation marker, native/result distinction, accumulator, and deciding guard in the current PS policy authority with isolated negative cases and valid/hostile controls.
- Coordinate with open PS #162. Do not weaken its no-publication boundary. If actual values cannot safely appear in the current repository-code job, implement and document a stronger compatible channel that preserves named distinctions and fixed ordering without exposing checkout-controlled content.
- Re-read the canonical baseline produced by Task 47 and current #162 body before design. Run both parsers, PSScriptAnalyzer, policy preflight and all cases once, hostile and valid controls, the complete two-run three-host generator matrix, generated-drift and exact-scope checks, current-head CI, review, and landed handoff.

### Procedure

1. If Task 50 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

## Task 52 — run the Codex review loop on the PS #171 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 51 PR URL | `{{TASK_051_PR_URL}}` | Produced by Task 51; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 51 completion record

~~~markdown
# Task 51 completion record

{{TASK_051_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 51 | `FS` | The focused repair PR is review-ready, and the current issue-ready branch is active. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 51 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 51's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 52 for the new head before Task 53. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

## Task 53 — run the independent final quality check on the PS #171 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 51 PR URL | `{{TASK_051_PR_URL}}` | Produced by Task 51; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 52 completion record

~~~markdown
# Task 52 completion record

{{TASK_052_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 52 | `FS` | A terminal-clean record applies to the current repair head/tree, and the current issue-ready branch is active. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 52 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 51's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 52 for the new head, and then repeat Task 53 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

## Task 54 — merge the PS #171 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 51 PR URL | `{{TASK_051_PR_URL}}` | Produced by Task 51; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 53 completion record

~~~markdown
# Task 53 completion record

{{TASK_053_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 53 | `FS` | Both review gates apply to the same current head/tree, and the current issue-ready branch is active. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 53 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

## Task 55 — publish the landed handoff for PS #171

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 51 PR URL | `{{TASK_051_PR_URL}}` | Produced by Task 51; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 54 completion record

~~~markdown
# Task 54 completion record

{{TASK_054_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 54 | `FS` | The repair PR is merged with an actual landed identity, and the current issue-ready branch is active. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 54 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

## Task 56 — perform the final reciprocal recheck for PS #169, #170, and #171

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

#### Task 29 initial cycle-2 comparison record

~~~markdown
# Task 29 initial cycle-2 comparison record

{{TASK_029_COMPLETION_RECORD}}
~~~

#### Task 38 PS #169 comparison record

~~~markdown
# Task 38 PS #169 comparison record

{{TASK_038_COMPLETION_RECORD}}
~~~

#### Task 47 PS #170 comparison record

~~~markdown
# Task 47 PS #170 comparison record

{{TASK_047_COMPLETION_RECORD}}
~~~

#### Task 55 PS #171 landed handoff

~~~markdown
# Task 55 PS #171 landed handoff

{{TASK_055_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 29 | `FS` | Use the original pinned cycle-2 comparison and immutable PS #161 and Terraform #34 inputs. |
| Task 38 | `FS` | PS #169 has its landed handoff and reciprocal comparison. |
| Task 47 | `FS` | PS #170 has its landed handoff and reciprocal comparison. |
| Task 55 | `FS` | PS #171 is closed by its reviewed merged PR and has an authenticated landed handoff. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat the applicable issue-through-handoff leaf range—Tasks 30–37 for #169, Tasks 39–46 for #170, or Tasks 48–55 for #171—as a new numbered-range instance; if Terraform is the target, create the equivalent eight discrete Terraform leaves before this recheck. After its new handoff, repeat Task 56.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 57 — publish the fixed-point closure for PS #169, #170, and #171

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 56 completion record

~~~markdown
# Task 56 completion record

{{TASK_056_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 56 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Required issue closure evidence

- PS #169, #170, and #171 are each closed by their own reviewed and quality-checked merged PR.
- Each PR has an authenticated landed handoff and a comparison tied to its exact landed commit and tree.
- The final comparison includes every requirement from all three issue bodies, every current intentional difference, and every effect of PS #162's future no-publication boundary.
- A tracker record must not close a still-open implementation issue or convert a cross-reference into a false dependency.

### Procedure

1. Re-query the final identities and the Task 56 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 58 — commence PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 57 completion record

~~~markdown
# Task 57 completion record

{{TASK_057_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 57 | `FS` | Cycle 2 has a permanent fixed-point record. |

### Objective

Explicitly commence only the implementation governed by implement PS workflow isolation and policy convergence. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final PS commit recorded by Task 57 is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 with Task 57's exact final PS commit before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

The landed #161 baseline already contains the v2 generator and exact-path verifier, the thin v2 `build.yml` caller, and their closed contract/validator identity integration. Treat those bytes as immutable starting state. Do not roll them back, relabel them as v1, or count that minimal integration as #162 implementation. This task owns only the broader #162 workflow topology, action-free repository-code boundary, Node acquisition, policy-case expansion, and isolation work stated below.

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 57 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from Task 57's final PS handoff before editing. Do not use the older PS #161 landing as the mutable editing baseline.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 59 — implement PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 58 completion record

~~~markdown
# Task 58 completion record

{{TASK_058_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 58 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS workflow isolation and policy convergence without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final PS commit recorded by Task 57 is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 with Task 57's exact final PS commit before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

The landed #161 baseline already contains the v2 generator and exact-path verifier, the thin v2 `build.yml` caller, and their closed contract/validator identity integration. Treat those bytes as immutable starting state. Do not roll them back, relabel them as v1, or count that minimal integration as #162 implementation. This task owns only the broader #162 workflow topology, action-free repository-code boundary, Node acquisition, policy-case expansion, and isolation work stated below.

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 57 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from Task 57's final PS handoff before editing. Do not use the older PS #161 landing as the mutable editing baseline.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Work from the exact commenced baseline in an isolated branch or worktree.
2. Implement the complete task-local contract and preserve every stated scope boundary.
3. Run all task-specific local validation, commit the candidate, and record the commit, tree, paths, blobs, runtimes, exit codes, and results. Do not push or update a PR.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 60 — create or update the candidate PR for PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 60 PR URL | `{{TASK_060_PR_URL}}` | Produced by this task; replace {{TASK_060_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 59 completion record

~~~markdown
# Task 59 completion record

{{TASK_059_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 59 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS workflow isolation and policy convergence; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final PS commit recorded by Task 57 is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 with Task 57's exact final PS commit before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

The landed #161 baseline already contains the v2 generator and exact-path verifier, the thin v2 `build.yml` caller, and their closed contract/validator identity integration. Treat those bytes as immutable starting state. Do not roll them back, relabel them as v1, or count that minimal integration as #162 implementation. This task owns only the broader #162 workflow topology, action-free repository-code boundary, Node acquisition, policy-case expansion, and isolation work stated below.

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 57 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from Task 57's final PS handoff before editing. Do not use the older PS #161 landing as the mutable editing baseline.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query the governing issue, remote branch, base branch, active implementation slot, and required checks.
2. Push the committed candidate safely. Create or update only one focused PR that links the issue and accurately describes the complete diff, evidence, security effects, intentional differences, rollback, and deferred-work disposition.
3. Read the PR back, record its base, head/tree, commits, paths and blobs, body edit state, and issue links. Require all initial checks to pass. Stop before review or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #162 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #162 candidate PR exists with complete initial validation and an accurate PR body.

## Task 61 — run the Codex review loop on the PS #162 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 60 PR URL | `{{TASK_060_PR_URL}}` | Produced by Task 60; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 60 completion record

~~~markdown
# Task 60 completion record

{{TASK_060_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 60 | `FS` | The PS #162 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the PS #162 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below against the PS #162 PR. Do not merge. If the loop changes bytes, rerun the Task 60 validation and continue on the new head.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 62 — run the independent final quality check on the PS #162 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 60 PR URL | `{{TASK_060_PR_URL}}` | Produced by Task 60; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 61 completion record

~~~markdown
# Task 61 completion record

{{TASK_061_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 61 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #162 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below against the PS #162 PR. Require full issue-contract, workflow-topology, policy-case, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 61 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 61.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 61.

## Task 63 — merge the PS #162 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 60 PR URL | `{{TASK_060_PR_URL}}` | Produced by Task 60; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 62 completion record

~~~markdown
# Task 62 completion record

{{TASK_062_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 62 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 64 — publish the landed handoff for the PS #162 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 60 PR URL | `{{TASK_060_PR_URL}}` | Produced by Task 60; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 63 completion record

~~~markdown
# Task 63 completion record

{{TASK_063_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 63 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #162 is closed and the final landed policy contract is ready for Task 73.

### Complete when

PS #162 is closed and the final landed policy contract is ready for Task 73.

## Task 65 — compare cycle 3 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 64 completion record

~~~markdown
# Task 64 completion record

{{TASK_064_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 64 | `FS` | Use the final landed PS #162 commit and policy contract. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 65 | Pin all Task 64 workflow, policy, contract, case, and validation blobs. |
| Task 65 | Complete the full foundation catalog and workflow-topology table in Terraform. |
| Task 65 | Apply all blockers from Task 64's required topology and policy. |
| Task 69 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 65 | Update PS #162, PS #159, and Terraform #31 with full identities and validation. |
| Task 65 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 65 | Map files by role, not only by filename. |
| Task 65 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 68 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 66 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 67 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 69 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 65 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 66 — create or update the focused repair issue for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 66 issue URL | `{{TASK_066_ISSUE_URL}}` | Produced by this task; replace {{TASK_066_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 65 completion record

~~~markdown
# Task 65 completion record

{{TASK_065_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 65 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 65's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 67 — commence the focused repair for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 66 issue URL | `{{TASK_066_ISSUE_URL}}` | Produced by Task 66; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 66 completion record

~~~markdown
# Task 66 completion record

{{TASK_066_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 66 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 66 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 68 — implement the focused repair for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 66 issue URL | `{{TASK_066_ISSUE_URL}}` | Produced by Task 66; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 67 completion record

~~~markdown
# Task 67 completion record

{{TASK_067_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 67 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 67 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 69 — create or update the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 66 issue URL | `{{TASK_066_ISSUE_URL}}` | Produced by Task 66; resolve globally before execution |
| Task 69 PR URL | `{{TASK_069_PR_URL}}` | Produced by this task; replace {{TASK_069_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 68 completion record

~~~markdown
# Task 68 completion record

{{TASK_068_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 68 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 68 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 70 — run the Codex review loop on the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 69 PR URL | `{{TASK_069_PR_URL}}` | Produced by Task 69; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 69 completion record

~~~markdown
# Task 69 completion record

{{TASK_069_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 69 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 69 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 69's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 70 for the new head before Task 71. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 71 — run the independent final quality check on the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 69 PR URL | `{{TASK_069_PR_URL}}` | Produced by Task 69; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 70 completion record

~~~markdown
# Task 70 completion record

{{TASK_070_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 70 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 70 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 69's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 70 for the new head, and then repeat Task 71 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 72 — merge the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 69 PR URL | `{{TASK_069_PR_URL}}` | Produced by Task 69; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 71 completion record

~~~markdown
# Task 71 completion record

{{TASK_071_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 71 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 71 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 73 — publish the landed repair handoff for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 69 PR URL | `{{TASK_069_PR_URL}}` | Produced by Task 69; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 72 completion record

~~~markdown
# Task 72 completion record

{{TASK_072_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 72 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 72 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 74 — reverse-compare cycle 3 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 73 completion record

~~~markdown
# Task 73 completion record

{{TASK_073_COMPLETION_RECORD}}
~~~

#### Task 64 completion record

~~~markdown
# Task 64 completion record

{{TASK_064_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 73 | `FS` | Use only the final landed commits from Tasks 64 and 73. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 74 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 74 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 75 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 76 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 78 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 83 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 77 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 84 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 84 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 75 — create or update the focused repair issue for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 75 issue URL | `{{TASK_075_ISSUE_URL}}` | Produced by this task; replace {{TASK_075_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 74 completion record

~~~markdown
# Task 74 completion record

{{TASK_074_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 74 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 74's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 76 — commence the focused repair for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 75 issue URL | `{{TASK_075_ISSUE_URL}}` | Produced by Task 75; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 75 completion record

~~~markdown
# Task 75 completion record

{{TASK_075_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 75 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 75 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 77 — implement the focused repair for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 75 issue URL | `{{TASK_075_ISSUE_URL}}` | Produced by Task 75; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 76 completion record

~~~markdown
# Task 76 completion record

{{TASK_076_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 76 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 76 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 78 — create or update the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 75 issue URL | `{{TASK_075_ISSUE_URL}}` | Produced by Task 75; resolve globally before execution |
| Task 78 PR URL | `{{TASK_078_PR_URL}}` | Produced by this task; replace {{TASK_078_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 77 completion record

~~~markdown
# Task 77 completion record

{{TASK_077_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 77 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 77 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 79 — run the Codex review loop on the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 78 PR URL | `{{TASK_078_PR_URL}}` | Produced by Task 78; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 78 completion record

~~~markdown
# Task 78 completion record

{{TASK_078_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 78 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 78 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 78's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 79 for the new head before Task 80. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 80 — run the independent final quality check on the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 78 PR URL | `{{TASK_078_PR_URL}}` | Produced by Task 78; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 79 completion record

~~~markdown
# Task 79 completion record

{{TASK_079_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 79 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 79 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 78's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 79 for the new head, and then repeat Task 80 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 81 — merge the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 78 PR URL | `{{TASK_078_PR_URL}}` | Produced by Task 78; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 80 completion record

~~~markdown
# Task 80 completion record

{{TASK_080_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 80 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 80 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 82 — publish the landed repair handoff for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 78 PR URL | `{{TASK_078_PR_URL}}` | Produced by Task 78; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 81 completion record

~~~markdown
# Task 81 completion record

{{TASK_081_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 81 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 81 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 83 — recheck the reciprocal result for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 74 completion record

~~~markdown
# Task 74 completion record

{{TASK_074_COMPLETION_RECORD}}
~~~

#### Task 82 completion record

~~~markdown
# Task 82 completion record

{{TASK_082_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 74 | `FS` | Use the original pinned comparison and branch result. |
| Task 82 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 68–82 as a new numbered-range instance. After its new handoff, repeat Task 83.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 84 — publish the fixed-point closure for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 83 completion record

~~~markdown
# Task 83 completion record

{{TASK_083_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 83 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 83 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 85 — post the initial three-cycle handoff

> **Execution class: Coding agent executable.** This is an evidence-publication task, not an implementation task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 84 completion record

~~~markdown
# Task 84 completion record

{{TASK_084_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 84 | `FS` | Cycles 1 through 3 have permanent fixed-point records. |

### Objective

Complete only this leaf action: post the initial three-cycle handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Post one interim handoff to PS #159 and Terraform #31. List cycles 1 through 3, every PS and Terraform issue and PR, bases, reviewed heads/trees, merge methods, landed commits/trees, source and destination blobs, matrices, validation, review outcomes, intentional differences, and fixed-point result. State that PS #158 remains nonblocking and has no native blocking edge to PS #147 or #152. Verify every identifier before posting. Do not close either tracker.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Both trackers contain a permanent, internally consistent handoff for cycles 1 through 3.

### Complete when

Both trackers contain a permanent, internally consistent handoff for cycles 1 through 3.

## Task 86 — update PS #158 with the landed workflow-policy starting point

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 85 completion record

~~~markdown
# Task 85 completion record

{{TASK_085_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 85 | `FS` | The final cycle 3 policy contract and interim handoff exist. |

### Objective

Update only the existing PS #158 issue record with the final landed workflow-policy starting point. Do not commence implementation.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #158, its parent #159, the landed workflow-policy PR and commit, and both repository main refs.
2. Replace `<PS_WORKFLOW_POLICY_PR_URL>` and `<PS_WORKFLOW_POLICY_LANDED_COMMIT>` in the task-local text below with the exact predecessor outputs.
3. Post the text as a planning comment without replacing the issue body. Read the comment back and verify both identities and the nonblocking scope.
4. Do not create a feature branch, edit implementation files, open a PR, or occupy the implementation slot.

### Exact issue update

Before implementation, replace both placeholders and post this planning comment to PS #158 without replacing its body:

> This issue is a child of [PS issue #159](https://github.com/franklesniak/PSStyleGuide/issues/159) and starts after `<PS_WORKFLOW_POLICY_PR_URL>` lands. Re-read the final landed `workflow-policy-contract.json` from `<PS_WORKFLOW_POLICY_LANDED_COMMIT>` before adapting the recorder. Preserve this issue's existing nonblocking scope and immutable Terraform PR #27 source pins.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #158 contains the verified starting-point comment, and implementation has not started.

### Complete when

PS #158 contains the verified starting-point comment, and implementation has not started.

## Task 87 — commence the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 86 completion record

~~~markdown
# Task 86 completion record

{{TASK_086_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 86 | `FS` | The issue update is complete and provides the starting point. |

### Objective

Explicitly commence only the implementation governed by implement the PS read-only supply-freeze recorder. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 88 — implement the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 87 completion record

~~~markdown
# Task 87 completion record

{{TASK_087_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 87 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement the PS read-only supply-freeze recorder without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-read the final Task 84 policy contract and the immutable Terraform PR #27 inputs.
2. Preserve the issue's existing nonblocking scope.
3. Keep the recorder read-only and capture its output outside the repository.
4. Prove that it does not mutate a manifest, lockfile, installed tree, contract, generated output, staged set, or worktree.
5. Complete a documented reduced reciprocal matrix for the narrow recorder surface. Include every applicable foundation row and prove why omitted rows are inapplicable.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 89 — create or update the candidate PR for the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 89 PR URL | `{{TASK_089_PR_URL}}` | Produced by this task; replace {{TASK_089_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 88 completion record

~~~markdown
# Task 88 completion record

{{TASK_088_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 88 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement the PS read-only supply-freeze recorder; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Create or update one focused PS #158 PR. Run initial validation and required checks.
2. Record the candidate head/tree, all source and candidate blobs, tool/runtime identities, exact output location, mutation-absence evidence, validation, and intentional differences. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #158 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #158 candidate PR exists with complete initial validation and an accurate PR body.

## Task 90 — run the Codex review loop on the PS #158 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 89 PR URL | `{{TASK_089_PR_URL}}` | Produced by Task 89; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 89 completion record

~~~markdown
# Task 89 completion record

{{TASK_089_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 89 | `FS` | The PS #158 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the PS #158 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below against the PS #158 PR. Do not merge. If the loop changes bytes, rerun the Task 89 validation.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 91 — run the independent final quality check on the PS #158 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 89 PR URL | `{{TASK_089_PR_URL}}` | Produced by Task 89; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 90 completion record

~~~markdown
# Task 90 completion record

{{TASK_090_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 90 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #158 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require PS #158 issue coverage, immutable-source verification, read-only and mutation-absence proof, comments, deferrals, PR-body accuracy, validation, and mergeability. Return to Task 90 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 90.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 90.

## Task 92 — merge the PS #158 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 89 PR URL | `{{TASK_089_PR_URL}}` | Produced by Task 89; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 91 completion record

~~~markdown
# Task 91 completion record

{{TASK_091_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 91 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 93 — publish the landed handoff for the PS #158 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 89 PR URL | `{{TASK_089_PR_URL}}` | Produced by Task 89; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 92 completion record

~~~markdown
# Task 92 completion record

{{TASK_092_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 92 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #158 is closed and its permanent handoff is ready for Terraform.

### Complete when

PS #158 is closed and its permanent handoff is ready for Terraform.

## Task 94 — compare cycle 4 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 93 completion record

~~~markdown
# Task 93 completion record

{{TASK_093_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 93 | `FS` | Use the final landed PS #158 recorder and handoff. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 94 | Pin the PS source and landed commits and blobs and the immutable Terraform PR #27 inputs. |
| Task 94 | Compare the read-only recorder roles with a documented reduced matrix. |
| Task 97 | Prove that both implementations preserve repository state and capture output outside the repository. |
| Task 98 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 94 | Update PS #158, PS #159, and Terraform #31 with all identities, validation, and differences. |
| Task 94 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 94 | Map files by role, not only by filename. |
| Task 94 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 97 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 95 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 96 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 98 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 94 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 95 — create or update the focused repair issue for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 95 issue URL | `{{TASK_095_ISSUE_URL}}` | Produced by this task; replace {{TASK_095_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 94 completion record

~~~markdown
# Task 94 completion record

{{TASK_094_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 94 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 94's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 96 — commence the focused repair for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 95 issue URL | `{{TASK_095_ISSUE_URL}}` | Produced by Task 95; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 95 completion record

~~~markdown
# Task 95 completion record

{{TASK_095_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 95 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 95 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 97 — implement the focused repair for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 95 issue URL | `{{TASK_095_ISSUE_URL}}` | Produced by Task 95; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 96 completion record

~~~markdown
# Task 96 completion record

{{TASK_096_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 96 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 96 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 98 — create or update the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 95 issue URL | `{{TASK_095_ISSUE_URL}}` | Produced by Task 95; resolve globally before execution |
| Task 98 PR URL | `{{TASK_098_PR_URL}}` | Produced by this task; replace {{TASK_098_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 97 completion record

~~~markdown
# Task 97 completion record

{{TASK_097_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 97 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 97 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 99 — run the Codex review loop on the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 98 PR URL | `{{TASK_098_PR_URL}}` | Produced by Task 98; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 98 completion record

~~~markdown
# Task 98 completion record

{{TASK_098_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 98 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 98 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 98's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 99 for the new head before Task 100. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 100 — run the independent final quality check on the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 98 PR URL | `{{TASK_098_PR_URL}}` | Produced by Task 98; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 99 completion record

~~~markdown
# Task 99 completion record

{{TASK_099_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 99 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 99 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 98's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 99 for the new head, and then repeat Task 100 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 101 — merge the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 98 PR URL | `{{TASK_098_PR_URL}}` | Produced by Task 98; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 100 completion record

~~~markdown
# Task 100 completion record

{{TASK_100_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 100 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 100 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 102 — publish the landed repair handoff for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 98 PR URL | `{{TASK_098_PR_URL}}` | Produced by Task 98; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 101 completion record

~~~markdown
# Task 101 completion record

{{TASK_101_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 101 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 101 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 103 — reverse-compare cycle 4 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 102 completion record

~~~markdown
# Task 102 completion record

{{TASK_102_COMPLETION_RECORD}}
~~~

#### Task 93 completion record

~~~markdown
# Task 93 completion record

{{TASK_093_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 102 | `FS` | Use only the final landed commits from Tasks 93 and 102. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 103 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 103 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 104 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 105 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 107 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 112 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 106 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 113 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 113 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 104 — create or update the focused repair issue for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 104 issue URL | `{{TASK_104_ISSUE_URL}}` | Produced by this task; replace {{TASK_104_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 103 completion record

~~~markdown
# Task 103 completion record

{{TASK_103_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 103 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 103's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 105 — commence the focused repair for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 104 issue URL | `{{TASK_104_ISSUE_URL}}` | Produced by Task 104; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 104 completion record

~~~markdown
# Task 104 completion record

{{TASK_104_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 104 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 104 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 106 — implement the focused repair for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 104 issue URL | `{{TASK_104_ISSUE_URL}}` | Produced by Task 104; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 105 completion record

~~~markdown
# Task 105 completion record

{{TASK_105_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 105 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 105 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 107 — create or update the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 104 issue URL | `{{TASK_104_ISSUE_URL}}` | Produced by Task 104; resolve globally before execution |
| Task 107 PR URL | `{{TASK_107_PR_URL}}` | Produced by this task; replace {{TASK_107_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 106 completion record

~~~markdown
# Task 106 completion record

{{TASK_106_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 106 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 106 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 108 — run the Codex review loop on the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 107 PR URL | `{{TASK_107_PR_URL}}` | Produced by Task 107; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 107 completion record

~~~markdown
# Task 107 completion record

{{TASK_107_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 107 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 107 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 107's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 108 for the new head before Task 109. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 109 — run the independent final quality check on the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 107 PR URL | `{{TASK_107_PR_URL}}` | Produced by Task 107; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 108 completion record

~~~markdown
# Task 108 completion record

{{TASK_108_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 108 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 108 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 107's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 108 for the new head, and then repeat Task 109 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 110 — merge the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 107 PR URL | `{{TASK_107_PR_URL}}` | Produced by Task 107; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 109 completion record

~~~markdown
# Task 109 completion record

{{TASK_109_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 109 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 109 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 111 — publish the landed repair handoff for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 107 PR URL | `{{TASK_107_PR_URL}}` | Produced by Task 107; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 110 completion record

~~~markdown
# Task 110 completion record

{{TASK_110_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 110 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 110 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 112 — recheck the reciprocal result for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 111 completion record

~~~markdown
# Task 111 completion record

{{TASK_111_COMPLETION_RECORD}}
~~~

#### Task 103 completion record

~~~markdown
# Task 103 completion record

{{TASK_103_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 103 | `FS` | Use the original pinned comparison and branch result. |
| Task 111 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 97–111 as a new numbered-range instance. After its new handoff, repeat Task 112.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 113 — publish the fixed-point closure for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 112 completion record

~~~markdown
# Task 112 completion record

{{TASK_112_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 112 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 112 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 114 — close the PS foundation umbrella

> **Execution class: Coding agent executable.** This is an issue-evidence and closure task, not implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #159 URL | `https://github.com/franklesniak/PSStyleGuide/issues/159` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 113 completion record

~~~markdown
# Task 113 completion record

{{TASK_113_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 113 | `FS` | PS #160, #161, #162, and #158 and all four reciprocal cycles are complete. |

### Objective

Complete only this leaf action: close the PS foundation umbrella. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #159 and its four native children.
2. Verify that #160, #161, #162, and #158 are closed and that every cycle has a permanent fixed-point record.
3. Post the foundation handoff. Include every issue and PR URL, base, reviewed head/tree, merge method, landed commit/tree, affected path and blob, complete or reduced matrix, validation command and result, runtime identity, review outcome, intentional difference, and reciprocal closure result.
4. Verify that Terraform #31 links the same four cycle records.
5. Close PS #159. Do not create or merge an aggregate foundation PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

PS #159 is closed with complete, matching evidence in both repositories.

### Complete when

PS #159 is closed with complete, matching evidence in both repositories.

## Task 115 — commence the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

In Tasks 115–117 and their comparison evidence, `Claude` names the command and compatibility artifact required by issue #163. It does not select the task executor. The parent still dispatches each coding-agent task, including the formal review of the resulting PR, to Codex.

#### Task 114 completion record

~~~markdown
# Task 114 completion record

{{TASK_114_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 114 | `FS` | The foundation umbrella is closed and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement the PS Claude review-loop command. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 116 — implement the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 115 completion record

~~~markdown
# Task 115 completion record

{{TASK_115_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 115 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement the PS Claude review-loop command without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Reconfirm the existing owner authorization because the new file is an agent entry point.
2. Add a thin, repository-local command wrapper.
3. Accept a pull-request URL. Require the URL when the caller does not supply it.
4. Delegate the authoritative process to the repository-local root `CLAUDE.md`.
5. Treat Codex and Copilot as co-equal reviewers, as `CLAUDE.md` requires.
6. Do not duplicate a step count, gate count, round limit, or other volatile protocol detail.
7. Do not fetch instructions from a moving external branch or add a shared runtime dependency.
8. Record a line-by-line comparison with the pinned Terraform input. Explain each intentional difference.
9. Complete a documented reduced reciprocal matrix. Run Markdown lint and all applicable local agent-instruction checks.
10. Keep the issue and PR separate from foundation, supply-freeze, and infrastructure-conformance work.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 117 — create or update the candidate PR for the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 117 PR URL | `{{TASK_117_PR_URL}}` | Produced by this task; replace {{TASK_117_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 116 completion record

~~~markdown
# Task 116 completion record

{{TASK_116_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 116 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement the PS Claude review-loop command; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Create or update one focused PS #163 PR. Record the candidate head/tree, pinned input, line comparison, validation, and initial checks. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #163 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #163 candidate PR exists with complete initial validation and an accurate PR body.

## Task 118 — run the Codex review loop on the PS #163 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 117 PR URL | `{{TASK_117_PR_URL}}` | Produced by Task 117; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 117 completion record

~~~markdown
# Task 117 completion record

{{TASK_117_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 117 | `FS` | The PS #163 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the PS #163 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below against the PS #163 PR. Do not merge. If the loop changes bytes, rerun the Task 117 validation.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 119 — run the independent final quality check on the PS #163 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 117 PR URL | `{{TASK_117_PR_URL}}` | Produced by Task 117; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 118 completion record

~~~markdown
# Task 118 completion record

{{TASK_118_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 118 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #163 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require PS #163 issue coverage, protected-entry-point authorization, pinned-input comparison, absence of volatile protocol duplication, comments, deferrals, PR-body accuracy, validation, and mergeability. Return to Task 118 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 118.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 118.

## Task 120 — merge the PS #163 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 117 PR URL | `{{TASK_117_PR_URL}}` | Produced by Task 117; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 119 completion record

~~~markdown
# Task 119 completion record

{{TASK_119_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 119 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 121 — publish the landed handoff for the PS #163 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 117 PR URL | `{{TASK_117_PR_URL}}` | Produced by Task 117; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 120 completion record

~~~markdown
# Task 120 completion record

{{TASK_120_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 120 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #163 is closed and its permanent handoff is ready for Terraform comparison.

### Complete when

PS #163 is closed and its permanent handoff is ready for Terraform comparison.

## Task 122 — compare cycle 5 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 121 completion record

~~~markdown
# Task 121 completion record

{{TASK_121_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 121 | `FS` | Use the final landed PS #163 command. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 122 | Pin the PS command and authoritative `CLAUDE.md` blobs and the immutable Terraform command blob. |
| Task 122 | Compare both thin wrappers line by line with a documented reduced matrix. |
| Task 122 | Preserve each repository's authoritative local instruction file and repository identity. |
| Task 126 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 122 | Update PS #163 and Terraform #31 with all identities, validation, and intentional differences. |
| Task 122 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 122 | Map files by role, not only by filename. |
| Task 122 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 125 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 123 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 124 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 126 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 122 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 123 — create or update the focused repair issue for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 123 issue URL | `{{TASK_123_ISSUE_URL}}` | Produced by this task; replace {{TASK_123_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 122 completion record

~~~markdown
# Task 122 completion record

{{TASK_122_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 122 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 122's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 124 — commence the focused repair for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 123 issue URL | `{{TASK_123_ISSUE_URL}}` | Produced by Task 123; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 123 completion record

~~~markdown
# Task 123 completion record

{{TASK_123_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 123 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 123 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 125 — implement the focused repair for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 123 issue URL | `{{TASK_123_ISSUE_URL}}` | Produced by Task 123; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 124 completion record

~~~markdown
# Task 124 completion record

{{TASK_124_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 124 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 124 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 126 — create or update the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 123 issue URL | `{{TASK_123_ISSUE_URL}}` | Produced by Task 123; resolve globally before execution |
| Task 126 PR URL | `{{TASK_126_PR_URL}}` | Produced by this task; replace {{TASK_126_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 125 completion record

~~~markdown
# Task 125 completion record

{{TASK_125_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 125 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 125 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 127 — run the Codex review loop on the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 126 PR URL | `{{TASK_126_PR_URL}}` | Produced by Task 126; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 126 completion record

~~~markdown
# Task 126 completion record

{{TASK_126_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 126 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 126 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 126's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 127 for the new head before Task 128. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 128 — run the independent final quality check on the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 126 PR URL | `{{TASK_126_PR_URL}}` | Produced by Task 126; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 127 completion record

~~~markdown
# Task 127 completion record

{{TASK_127_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 127 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 127 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 126's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 127 for the new head, and then repeat Task 128 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 129 — merge the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 126 PR URL | `{{TASK_126_PR_URL}}` | Produced by Task 126; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 128 completion record

~~~markdown
# Task 128 completion record

{{TASK_128_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 128 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 128 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 130 — publish the landed repair handoff for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 126 PR URL | `{{TASK_126_PR_URL}}` | Produced by Task 126; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 129 completion record

~~~markdown
# Task 129 completion record

{{TASK_129_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 129 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 129 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 131 — reverse-compare cycle 5 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 130 completion record

~~~markdown
# Task 130 completion record

{{TASK_130_COMPLETION_RECORD}}
~~~

#### Task 121 completion record

~~~markdown
# Task 121 completion record

{{TASK_121_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 130 | `FS` | Use only the final landed commits from Tasks 121 and 130. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 131 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 131 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 132 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 133 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 135 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 140 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 134 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 141 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 141 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 132 — create or update the focused repair issue for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 132 issue URL | `{{TASK_132_ISSUE_URL}}` | Produced by this task; replace {{TASK_132_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 131 completion record

~~~markdown
# Task 131 completion record

{{TASK_131_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 131 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 131's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 133 — commence the focused repair for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 132 issue URL | `{{TASK_132_ISSUE_URL}}` | Produced by Task 132; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 132 completion record

~~~markdown
# Task 132 completion record

{{TASK_132_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 132 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 132 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 134 — implement the focused repair for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 132 issue URL | `{{TASK_132_ISSUE_URL}}` | Produced by Task 132; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 133 completion record

~~~markdown
# Task 133 completion record

{{TASK_133_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 133 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 133 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 135 — create or update the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 132 issue URL | `{{TASK_132_ISSUE_URL}}` | Produced by Task 132; resolve globally before execution |
| Task 135 PR URL | `{{TASK_135_PR_URL}}` | Produced by this task; replace {{TASK_135_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 134 completion record

~~~markdown
# Task 134 completion record

{{TASK_134_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 134 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 134 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 136 — run the Codex review loop on the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 135 PR URL | `{{TASK_135_PR_URL}}` | Produced by Task 135; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 135 completion record

~~~markdown
# Task 135 completion record

{{TASK_135_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 135 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 135 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 135's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 136 for the new head before Task 137. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 137 — run the independent final quality check on the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 135 PR URL | `{{TASK_135_PR_URL}}` | Produced by Task 135; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 136 completion record

~~~markdown
# Task 136 completion record

{{TASK_136_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 136 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 136 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 135's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 136 for the new head, and then repeat Task 137 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 138 — merge the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 135 PR URL | `{{TASK_135_PR_URL}}` | Produced by Task 135; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 137 completion record

~~~markdown
# Task 137 completion record

{{TASK_137_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 137 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 137 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 139 — publish the landed repair handoff for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 135 PR URL | `{{TASK_135_PR_URL}}` | Produced by Task 135; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 138 completion record

~~~markdown
# Task 138 completion record

{{TASK_138_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 138 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 138 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 140 — recheck the reciprocal result for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 131 completion record

~~~markdown
# Task 131 completion record

{{TASK_131_COMPLETION_RECORD}}
~~~

#### Task 139 completion record

~~~markdown
# Task 139 completion record

{{TASK_139_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 131 | `FS` | Use the original pinned comparison and branch result. |
| Task 139 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 125–139 as a new numbered-range instance. After its new handoff, repeat Task 140.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 141 — publish the fixed-point closure for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 140 completion record

~~~markdown
# Task 140 completion record

{{TASK_140_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 140 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 140 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 142 — close the Terraform initial-convergence tracker

> **Execution class: Coding agent executable.** This is an issue-evidence and closure task, not implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #31 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/31` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 141 completion record

~~~markdown
# Task 141 completion record

{{TASK_141_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 141 | `FS` | All five initial reciprocal cycles have permanent closure records. |

### Objective

Complete only this leaf action: close the Terraform initial-convergence tracker. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Tracker contract

Terraform #31 retains evidence; it does not authorize an aggregate implementation PR. Its five ordered records are:

1. PS infrastructure conformance from Terraform PR #30.
2. PS generator and path convergence from Terraform PRs #26 and #30.
3. PS workflow and policy convergence from Terraform PRs #26 and #30.
4. PS #158 supply-freeze adaptation from Terraform PR #27.
5. PS Claude-command parity from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`.

### Procedure

1. Re-query Terraform #31.
2. Verify that each cycle record contains the PS issue and landed PR; source and destination commits and blobs; a complete foundation matrix or justified reduced matrix; every focused Terraform sync issue and PR; the reverse PS comparison; validation and intentional differences; and the permanent fixed-point result.
3. Reconcile the tracker against PS #159 and PS #163. Correct a record only with verified evidence.
4. Close Terraform #31. Do not create or merge an aggregate foundation implementation PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Terraform #31 is closed and its five records match the permanent records in PSStyleGuide.

### Complete when

Terraform #31 is closed and its five records match the permanent records in PSStyleGuide.

## Task 143 — commence Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 142 completion record

~~~markdown
# Task 142 completion record

{{TASK_142_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 142 | `FS` | The five initial cycles are closed and the final Terraform baseline is known. |

Terraform #21 exists, but its creation did not start implementation. Do not start Terraform #22 or #24 in this task.

### Objective

Explicitly commence only the implementation governed by implement Terraform #21 from the landed PS P1A source. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 142 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Verify the five Task 142 closure records and the free implementation slot.
2. Explicitly commence only Terraform #21.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 144 — implement Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 143 completion record

~~~markdown
# Task 143 completion record

{{TASK_143_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 143 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #21 from the landed PS P1A source without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 142 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Re-query Terraform #21. Record its four source blobs, four destination paths, source and destination identities, and expected repository-specific differences.
2. Classify the capability as common.
3. Adapt the landed PS implementation. Do not redesign it independently or introduce a shared dependency.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 145 — create or update the candidate PR for Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 145 PR URL | `{{TASK_145_PR_URL}}` | Produced by this task; replace {{TASK_145_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 144 completion record

~~~markdown
# Task 144 completion record

{{TASK_144_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 144 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #21 from the landed PS P1A source; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 142 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Create or update one focused Terraform #21 PR. Run initial local validation and required checks.
2. Record its base, candidate head/tree, affected paths and destination blobs, validation, runtime identities, and issue links. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #21 candidate PR exists with complete initial validation and an accurate PR body. Keep #21 open.

### Complete when

The Terraform #21 candidate PR exists with complete initial validation and an accurate PR body. Keep #21 open.

## Task 146 — run the Codex review loop on the Terraform #21 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 145 PR URL | `{{TASK_145_PR_URL}}` | Produced by Task 145; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 145 completion record

~~~markdown
# Task 145 completion record

{{TASK_145_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 145 | `FS` | The Terraform #21 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the Terraform #21 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below in TerraformStyleGuide against the Terraform #21 PR. Apply that repository's current root `AGENTS.md` instructions, or read its root `CLAUDE.md` as compatibility workflow instructions when `AGENTS.md` is absent. Do not merge. If the loop changes bytes, rerun the Task 145 validation.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 147 — run the independent final quality check on the Terraform #21 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 145 PR URL | `{{TASK_145_PR_URL}}` | Produced by Task 145; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 146 completion record

~~~markdown
# Task 146 completion record

{{TASK_146_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 146 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #21 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require all Terraform #21 requirements, four-path scope, 115/141-row allocation, comments, deferrals, PR-body accuracy, validation, checks, and mergeability. Return to Task 146 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 146.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 146.

## Task 148 — merge the Terraform #21 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 145 PR URL | `{{TASK_145_PR_URL}}` | Produced by Task 145; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 147 completion record

~~~markdown
# Task 147 completion record

{{TASK_147_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 147 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 149 — publish the landed handoff for the Terraform #21 implementation

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 145 PR URL | `{{TASK_145_PR_URL}}` | Produced by Task 145; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 148 completion record

~~~markdown
# Task 148 completion record

{{TASK_148_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 148 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #21 implementation is landed and its permanent implementation handoff contains every required identity and evidence item.

### Complete when

The Terraform #21 implementation is landed and its permanent implementation handoff contains every required identity and evidence item.

## Task 150 — reverse-compare Terraform #21 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 149 completion record

~~~markdown
# Task 149 completion record

{{TASK_149_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 149 | `FS` | Use the landed Terraform #21 result and immutable PS P1A commit. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 150 | Replace every placeholder in the reverse-comparison record with a verified identity. |
| Task 150 | Read PS only from `2d56357d9f52c76734027174bf62278e6f3d4cd6` and Terraform only from the Task 149 landed commit. Record every compared blob and map files by role. |
| Task 150 | Complete every applicable foundation row and the exact four-file mapping. Preserve the stated 115-row PS and 141-row Terraform allocations. |
| Task 150 | If no common difference remains, make no PS change and post a pinned reverse-comparison record. |
| Task 151 | If Terraform added or corrected common behavior, create or update one focused PS sync-back issue. Pin both landed commits and blobs. |
| Task 152 | Start the sync-back only after issue setup and a free implementation slot. Prepare one focused PS PR and run the four task-local conditional repair PR lifecycle actions. |
| Task 150 | If PS changes, compare its new landed commit in Terraform. For a required Terraform repair, run the same four task-local conditional repair PR lifecycle actions. |
| Task 150 | Stop for a new decision process if a row changes direction twice. |
| Task 150 | Post the current reciprocal evidence on Terraform #21 and the PS P1A handoff. Do not close Terraform #21 until Task 169 confirms the final Terraform state. |
| Task 150 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 150 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 151 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 152 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 154 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 150 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 153 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 150 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 150 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 151 — create or update the focused repair issue for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 150 completion record

~~~markdown
# Task 150 completion record

{{TASK_150_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 150 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 150's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 152 — commence the focused repair for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 151 completion record

~~~markdown
# Task 151 completion record

{{TASK_151_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 151 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 151 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 153 — implement the focused repair for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 152 completion record

~~~markdown
# Task 152 completion record

{{TASK_152_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 152 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 152 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 154 — create or update the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 154 PR URL | `{{TASK_154_PR_URL}}` | Produced by this task; replace {{TASK_154_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 153 completion record

~~~markdown
# Task 153 completion record

{{TASK_153_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 153 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 153 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 155 — run the Codex review loop on the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 154 PR URL | `{{TASK_154_PR_URL}}` | Produced by Task 154; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 154 completion record

~~~markdown
# Task 154 completion record

{{TASK_154_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 154 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 154 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 154's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 155 for the new head before Task 156. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 156 — run the independent final quality check on the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 154 PR URL | `{{TASK_154_PR_URL}}` | Produced by Task 154; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 155 completion record

~~~markdown
# Task 155 completion record

{{TASK_155_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 155 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 155 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 154's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 155 for the new head, and then repeat Task 156 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 157 — merge the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 154 PR URL | `{{TASK_154_PR_URL}}` | Produced by Task 154; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 156 completion record

~~~markdown
# Task 156 completion record

{{TASK_156_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 156 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 156 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 158 — publish the landed repair handoff for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 154 PR URL | `{{TASK_154_PR_URL}}` | Produced by Task 154; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 157 completion record

~~~markdown
# Task 157 completion record

{{TASK_157_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 157 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 157 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 159 — perform the final Terraform recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 158 completion record

~~~markdown
# Task 158 completion record

{{TASK_158_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 158 | `FS` | Use the final landed PS state from Task 158. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 159 | Compare the final Task 158 PS landed commit against Terraform. |
| Task 160 | If a common difference remains, create a focused Terraform issue before implementation. Use the task-local execution controls, conditional repair PR lifecycle, and final reciprocal-recheck controls below until the row is `same` or a proved intentional difference. |
| Task 159 | Verify that every common row is `same`, every intentional difference has complete evidence, and no row is a blocker. |
| Task 169 | Post one permanent closure record on Terraform #21 and the PS P1A handoff. Include both repositories' final issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, affected paths, source and destination blobs, final matrix digest, validation, runtime identities, review outcomes, and explicit non-applicability results. |
| Task 159 | Close Terraform #21. |
| Task 159 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 159 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 160 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 162 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 169 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 169 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 160 — create or update the focused repair issue for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 160 issue URL | `{{TASK_160_ISSUE_URL}}` | Produced by this task; replace {{TASK_160_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 159 completion record

~~~markdown
# Task 159 completion record

{{TASK_159_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 159 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 159's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 161 — commence the focused repair for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 160 issue URL | `{{TASK_160_ISSUE_URL}}` | Produced by Task 160; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 160 completion record

~~~markdown
# Task 160 completion record

{{TASK_160_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 160 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 160 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 162 — implement the focused repair for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 160 issue URL | `{{TASK_160_ISSUE_URL}}` | Produced by Task 160; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 161 completion record

~~~markdown
# Task 161 completion record

{{TASK_161_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 161 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 161 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 163 — create or update the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 160 issue URL | `{{TASK_160_ISSUE_URL}}` | Produced by Task 160; resolve globally before execution |
| Task 163 PR URL | `{{TASK_163_PR_URL}}` | Produced by this task; replace {{TASK_163_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 162 completion record

~~~markdown
# Task 162 completion record

{{TASK_162_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 162 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 162 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 164 — run the Codex review loop on the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 163 PR URL | `{{TASK_163_PR_URL}}` | Produced by Task 163; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 163 completion record

~~~markdown
# Task 163 completion record

{{TASK_163_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 163 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 163 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 163's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 164 for the new head before Task 165. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 165 — run the independent final quality check on the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 163 PR URL | `{{TASK_163_PR_URL}}` | Produced by Task 163; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 164 completion record

~~~markdown
# Task 164 completion record

{{TASK_164_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 164 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 164 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 163's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 164 for the new head, and then repeat Task 165 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 166 — merge the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 163 PR URL | `{{TASK_163_PR_URL}}` | Produced by Task 163; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 165 completion record

~~~markdown
# Task 165 completion record

{{TASK_165_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 165 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 165 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 167 — publish the landed repair handoff for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 163 PR URL | `{{TASK_163_PR_URL}}` | Produced by Task 163; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 166 completion record

~~~markdown
# Task 166 completion record

{{TASK_166_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 166 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 166 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 168 — recheck the reciprocal result for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 159 completion record

~~~markdown
# Task 159 completion record

{{TASK_159_COMPLETION_RECORD}}
~~~

#### Task 167 completion record

~~~markdown
# Task 167 completion record

{{TASK_167_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 159 | `FS` | Use the original pinned comparison and branch result. |
| Task 167 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 153–167 as a new numbered-range instance. After its new handoff, repeat Task 168.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 169 — publish the fixed-point closure for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 168 completion record

~~~markdown
# Task 168 completion record

{{TASK_168_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 168 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 168 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 170 — inventory all untracked cross-repository differences

> **Execution class: Coding agent executable.** This is read-only analysis and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 169 completion record

~~~markdown
# Task 169 completion record

{{TASK_169_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 169 | `FS` | The P1A/Terraform #21 reciprocal cycle is closed. |

### Objective

Complete only this leaf action: inventory all untracked cross-repository differences. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch both current `main` commits and trees in clean checkouts. Pin them as the sweep baselines.
2. Inventory every path that controls generation, validation, workflow policy, Actions permissions and pins, Node/npm acquisition, Markdown lint, Git/path checks, supply-freeze evidence, Dependabot, agent review commands, issue handoffs, and contributor instructions.
3. Map files by role. Do not infer role equality or difference from filenames alone.
4. For every common role, compare bytes and semantics, success and negative cases, failure classification, cleanup, credentials, permissions, native statuses, platform coverage, evidence retention, documentation, and validation commands.
5. Run the full foundation catalog for foundation roles. Use a documented reduced matrix only for a narrow non-foundation role.
6. Classify each row as `same`, `intentional difference`, `tracked work`, or `untracked blocker`.
7. Accept an intentional difference only under the task-local intentional-difference evidence requirements below. Link the owning issue for every `tracked work` row.
8. Treat a missing, duplicate, unknown, renamed, empty, or unexplained row as an `untracked blocker`.
9. For each untracked blocker, validate the finding and complete the required finding-specific decision process before proposing a repair.
10. Publish a read-only inventory that identifies the behind repository, affected role and blobs, evidence, owner/disposition, and proposed focused issue for each blocker. Do not edit implementation files in this task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Every inventoried role has one supported classification and owner. The report contains both baseline identities, file/blob maps, matrices and digests, evidence, and the exact ordered list of untracked blockers.

### Complete when

Every inventoried role has one supported classification and owner. The report contains both baseline identities, file/blob maps, matrices and digests, evidence, and the exact ordered list of untracked blockers.

## Task 171 — assess one untracked-blocker reciprocal cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | This disposition task reads both repositories |
| Selected repair target | `{{TASK_171_SELECTED_TARGET_REPOSITORY}}` | Produced by this task when repair is required; replace globally when known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 170 completion record

~~~markdown
# Task 170 completion record

{{TASK_170_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 170 | `FS` | The complete read-only inventory and ordered blocker list exist. |
| Prior completed instance of Tasks 164–181 | `FS` | Close the prior repair cycle before the next instance starts. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 171 | Select the next untracked blocker in the published order. |
| Task 172 | Create or update one focused issue in the behind repository. Pin the other repository's landed commit and source blobs. |
| Task 173 | Issue creation does not start implementation. Start only after issue setup and a free implementation slot. |
| Task 171 | Keep both repositories self-contained. Do not combine unrelated fixes or change settings without exact authorization. |
| Task 175 | Prepare one focused repair PR. Run the candidate, Codex review-loop, independent final quality-check, and merge actions as discrete lifecycle steps. |
| Task 180 | Compare the landed repair in the other repository. Post a pinned no-change record or run the same four discrete steps for one focused reciprocal repair. |
| Task 171 | Run the reverse comparison. Stop for a new decision process if a row changes direction twice. |
| Task 181 | Close the cycle only when every common row is `same`, all intentional differences have complete evidence, and no blocker remains. |
| Task 181 | Post the permanent closure record with both repositories' final issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, paths, blobs, matrix digest, validation, runtimes, review outcomes, and non-applicability results. |
| Task 171 | Repeat Task 181 only if another ordered blocker remains. |
| Task 171 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 171 | Map files by role, not only by filename. |
| Task 171 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 174 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 172 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 173 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 175 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 180 | Compare the landed destination result back against the source repository. |
| Task 172 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 175 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 174 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 181 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 181 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 172 — create or update the focused repair issue for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_171_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 171; resolve globally before execution |
| Task 172 issue URL | `{{TASK_172_ISSUE_URL}}` | Produced by this task; replace {{TASK_172_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 171 completion record

~~~markdown
# Task 171 completion record

{{TASK_171_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 171 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 171's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 173 — commence the focused repair for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_171_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 171; resolve globally before execution |
| Task 172 issue URL | `{{TASK_172_ISSUE_URL}}` | Produced by Task 172; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 172 completion record

~~~markdown
# Task 172 completion record

{{TASK_172_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 172 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 172 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 174 — implement the focused repair for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_171_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 171; resolve globally before execution |
| Task 172 issue URL | `{{TASK_172_ISSUE_URL}}` | Produced by Task 172; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 173 completion record

~~~markdown
# Task 173 completion record

{{TASK_173_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 173 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 173 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 175 — create or update the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_171_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 171; resolve globally before execution |
| Task 172 issue URL | `{{TASK_172_ISSUE_URL}}` | Produced by Task 172; resolve globally before execution |
| Task 175 PR URL | `{{TASK_175_PR_URL}}` | Produced by this task; replace {{TASK_175_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 174 completion record

~~~markdown
# Task 174 completion record

{{TASK_174_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 174 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 174 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 176 — run the Codex review loop on the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_171_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 171; resolve globally before execution |
| Task 175 PR URL | `{{TASK_175_PR_URL}}` | Produced by Task 175; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 175 completion record

~~~markdown
# Task 175 completion record

{{TASK_175_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 175 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 175 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 175's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 176 for the new head before Task 177. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 177 — run the independent final quality check on the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_171_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 171; resolve globally before execution |
| Task 175 PR URL | `{{TASK_175_PR_URL}}` | Produced by Task 175; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 176 completion record

~~~markdown
# Task 176 completion record

{{TASK_176_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 176 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 176 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 175's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 176 for the new head, and then repeat Task 177 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 178 — merge the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_171_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 171; resolve globally before execution |
| Task 175 PR URL | `{{TASK_175_PR_URL}}` | Produced by Task 175; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 177 completion record

~~~markdown
# Task 177 completion record

{{TASK_177_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 177 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 177 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 179 — publish the landed repair handoff for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_171_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 171; resolve globally before execution |
| Task 175 PR URL | `{{TASK_175_PR_URL}}` | Produced by Task 175; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 178 completion record

~~~markdown
# Task 178 completion record

{{TASK_178_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 178 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 178 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 180 — recheck the reciprocal result for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 171 completion record

~~~markdown
# Task 171 completion record

{{TASK_171_COMPLETION_RECORD}}
~~~

#### Task 179 completion record

~~~markdown
# Task 179 completion record

{{TASK_179_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 171 | `FS` | Use the original pinned comparison and branch result. |
| Task 179 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 165–179 as a new numbered-range instance. After its new handoff, repeat Task 180.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 181 — publish the fixed-point closure for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 180 completion record

~~~markdown
# Task 180 completion record

{{TASK_180_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 180 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 180 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 182 — finalize the global consistency sweep

> **Execution class: Coding agent executable.** This is a verification and evidence-publication task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 170 completion record

~~~markdown
# Task 170 completion record

{{TASK_170_COMPLETION_RECORD}}
~~~

#### Task 181 completion record

~~~markdown
# Task 181 completion record

{{TASK_181_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 170 | `FS` | The inventory exists. |
| Every applicable Task 181 instance | `FS` | All untracked blocker cycles are closed. |

### Objective

Complete only this leaf action: finalize the global consistency sweep. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch both final `main` commits and trees.
2. Re-run the complete inventory, file-role map, foundation catalog, and every required reduced matrix.
3. Verify that every role has an owner and disposition, each `tracked work` row links its live planned issue, and no `untracked blocker` remains.
4. Post the final sweep record. Include final commits/trees, file/blob map, matrices and digests, validation, runtimes, intentional differences, explicit non-applicability results, and zero untracked blockers.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The permanent global-sweep record proves zero untracked blockers and identifies every remaining planned work item.

### Complete when

The permanent global-sweep record proves zero untracked blockers and identifies every remaining planned work item.

## Task 183 — commence the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 182 completion record

~~~markdown
# Task 182 completion record

{{TASK_182_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 182 | `FS` | The global sweep proves zero untracked blockers. |

### Objective

Explicitly commence only the implementation governed by prepare the PS P1B writer candidate and decision evidence. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Verify that the implementation slot is free. Explicitly start only #147.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 184 — prepare the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 183 completion record

~~~markdown
# Task 183 completion record

{{TASK_183_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 183 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate prepare the PS P1B writer candidate and decision evidence without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-read the final PS foundation handoff and every landed contract.
2. Reconcile each PS #147 assumption with those contracts.
3. Implement the writer and terminal check `Build Style Guide Artifacts / approve_candidate`.
4. Prepare an amendment or replacement for `docs/decisions/0001-accept-in-repository-trust-root.md`.
5. Record workflow identity, permissions, required-check name, unique evidence ref, source pins, and rollback inputs.
6. Prepare the exact temporary and persistent settings requests and proof plan for PS #152.
7. Do not give the writer effective `contents: write` access before Task 189 records human approval.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 185 — create or update the candidate PR for the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by this task; replace {{TASK_185_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 184 completion record

~~~markdown
# Task 184 completion record

{{TASK_184_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 184 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for prepare the PS P1B writer candidate and decision evidence; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one PS #147 PR. Reach one locally validated candidate head. Record its exact head and tree.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One locally validated P1B candidate and the complete decision/settings evidence package are ready. Keep the PR unmerged.

### Complete when

One locally validated P1B candidate and the complete decision/settings evidence package are ready. Keep the PR unmerged.

## Task 186 — prepare read-only PS #152 settings evidence

> **Execution class: Coding agent executable.** This is read-only preparation and starts after Task 185 completes.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 183 completion record

~~~markdown
# Task 183 completion record

{{TASK_183_COMPLETION_RECORD}}
~~~

#### Task 185 completion record

~~~markdown
# Task 185 completion record

{{TASK_185_COMPLETION_RECORD}}
~~~

#### Task 186 authorization record

~~~markdown
# Task 186 authorization record

{{TASK_186_AUTHORIZATION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 185 | `FS` | Begin after the candidate PR is complete and its exact identity is available in the Task 185 completion record. |

### Objective

Complete only this leaf action: prepare read-only PS #152 settings evidence. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #152 and current rules, classic branch protection, `main`, application identity, and writer permissions.
2. Preserve the Terraform PR #29 bypass-eligibility finding and the limited 2026-08-01 approval scope.
3. Map every requested temporary and persistent setting to the Task 185 candidate.
4. Prepare the non-mutating bypass-eligibility check, field-equivalent temporary-rule request, effective-bypass and rejection drills, evidence-retention plan, cleanup proof, exact-restoration proof, persistent-ruleset request, and rollback plan.
5. Identify every decision that needs owner or administrator approval. Do not infer approval from prior evidence.
6. Do not change a setting, create a rule, or give the writer effective write access.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

PS #152 contains a complete, current, read-only evidence package that an owner can approve or reject without guessing.

### Complete when

PS #152 contains a complete, current, read-only evidence package that an owner can approve or reject without guessing.

## Task 187 — run the Codex review loop on the PS #147 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 185 completion record

~~~markdown
# Task 185 completion record

{{TASK_185_COMPLETION_RECORD}}
~~~

#### Task 186 completion record

~~~markdown
# Task 186 completion record

{{TASK_186_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 185 | `FS` | The PS #147 candidate PR exists and initial checks pass. |
| Task 186 | `FS` | The read-only PS #152 evidence package is complete. |

### Objective

Complete only this leaf action: run the Codex review loop on the PS #147 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below against the PS #147 PR. Do not merge or change repository settings. If the loop changes bytes or the writer design, rerun the applicable Task 185 validation and refresh Task 186 settings evidence before the next round.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree, and the PS #152 evidence still maps to that exact candidate.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree, and the PS #152 evidence still maps to that exact candidate.

## Task 188 — run the independent final quality check on the PS #147 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 187 completion record

~~~markdown
# Task 187 completion record

{{TASK_187_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 187 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #147 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require full PS #147 issue coverage; writer, evidence, lease/refspec, credential, terminal-result, rollback, comment, deferral, PR-body, validation, and mergeability checks; and consistency with the PS #152 settings request. Return to Task 187 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 187, and the settings evidence still applies.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 187, and the settings evidence still applies.

## Task 189 — approve or reject the P1B authority decisions

> **Execution class: Human execution required.** The repository owner or administrator must decide each item.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 186 completion record

~~~markdown
# Task 186 completion record

{{TASK_186_COMPLETION_RECORD}}
~~~

#### Task 188 completion record

~~~markdown
# Task 188 completion record

{{TASK_188_COMPLETION_RECORD}}
~~~

#### Task 189 authorization record

~~~markdown
# Task 189 authorization record

{{TASK_189_AUTHORIZATION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 186 | `FS` | Review the exact settings requests and proof plan. |
| Task 188 | `FS` | Review the exact candidate bytes and independent quality-check result. |

### Objective

Complete only this leaf action: approve or reject the P1B authority decisions. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Human actions:**

1. Review and approve or reject the proposed trust-root decision.
2. Review and approve or reject each temporary and persistent PS #152 settings request.
3. If integration ID `15368` is unavailable, select and approve another writer design.
4. Do not approve a substitute rule without also approving the writer design.
5. If the design permits human bypass, approve or reject that bypass in a separate decision.
6. Record every approval or rejection, condition, owner, canonical location, and expiry.
7. Do not reuse an approval for different bytes or settings.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The canonical records contain an explicit disposition for every required decision. A rejection sends the affected design back to Task 185 or Task 186 before work continues. After a candidate-byte change, rerun Tasks 187 and 185.

### Complete when

The canonical records contain an explicit disposition for every required decision. A rejection sends the affected design back to Task 185 or Task 186 before work continues. After a candidate-byte change, rerun Tasks 187 and 185.

## Task 190 — prove and activate the approved PS #152 settings

> **Execution class: Coding agent executable.** Recorded human approval is a required input. Do not merge PS #147 in this task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 189 completion record

~~~markdown
# Task 189 completion record

{{TASK_189_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 189 | `FS` | Every applicable approval exists and applies to the current candidate and exact settings. |

### Objective

Complete only this leaf action: prove and activate the approved PS #152 settings. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Verify all approvals. Stop if one is absent, expired, conditional on unmet work, or for different bytes or settings.
2. Revalidate the reviewed PS #147 head.
3. Start the bounded temporary proof under PS #152.
4. Export current rules, classic protection, `main`, application identity, and writer permissions.
5. Perform the non-mutating bypass-eligibility check.
6. If integration ID `15368` is unavailable, stop and return to Task 189.
7. Create the approved temporary field-equivalent rule.
8. Run the effective-bypass and rejection drills. Retain the required evidence.
9. Delete the temporary rule and evidence ref. Prove exact restoration.
10. Revalidate the reviewed PS #147 head.
11. As the final settings action, create the approved persistent ruleset immediately before Task 192.
12. Retain the create response, ruleset ID, normalized JSON, digest, effective rules, exact required check and integration, `main` before and after the setting change, and rollback proof.
13. Close PS #152 only after the persistent rule is active and evidence is complete.
14. Do not copy Terraform ruleset IDs, digests, effective-rule JSON, or audit records.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

PS #152 is closed with a proved active persistent rule, complete retained evidence, exact restoration proof for temporary state, and rollback proof.

### Complete when

PS #152 is closed with a proved active persistent rule, complete retained evidence, exact restoration proof for temporary state, and rollback proof.

## Task 191 — merge PS P1B

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 190 completion record

~~~markdown
# Task 190 completion record

{{TASK_190_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 190 | `FS` | PS #152 is closed and the approved persistent rule is active. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Revalidate the reviewed PS #147 head, checks, workflow identity, required check, active ruleset, application identity, and approvals.
2. Stop if any byte, setting, identity, or approval differs from the proved state.
3. Apply the task-local merge gate below. Require the Task 187 terminal-clean record and Task 188 independent `PASS` record to match the current head and tree.
4. Merge the reviewed P1B PR and close PS #147.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 192 — publish the landed handoff for PS P1B

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 191 completion record

~~~markdown
# Task 191 completion record

{{TASK_191_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 191 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Post the permanent P1B handoff for Terraform #22. Include all source and landed identities, paths and blobs, permission and credential boundaries, candidate consumption, ancestry/lease/refspec and native-status behavior, failure and cleanup postconditions, ruleset evidence, rollback proof, validation, review-loop result, final quality-check result, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #147 is closed by the proved merge and the permanent landed handoff is sufficient to implement Terraform #22 without redesign.

### Complete when

PS #147 is closed by the proved merge and the permanent landed handoff is sufficient to implement Terraform #22 without redesign.

## Task 193 — update Terraform #22 from the landed PS P1B handoff

> **Execution class: Coding agent executable.** This is issue setup and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 169 completion record

~~~markdown
# Task 169 completion record

{{TASK_169_COMPLETION_RECORD}}
~~~

#### Task 192 completion record

~~~markdown
# Task 192 completion record

{{TASK_192_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 192 | `FS` | Use the landed PS P1B commit and permanent handoff. |
| Task 169 | `FS` | Terraform #21 is closed. |

### Objective

Complete only this leaf action: update Terraform #22 from the landed PS P1B handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Issue update:**

Replace every placeholder with a verified identity and add or amend a `Starting point — reuse landed PSStyleGuide P1B` section in Terraform #22:

> PSStyleGuide P1B landed through `<PS_P1B_PR_URL>` at `<PS_P1B_LANDED_COMMIT>`, tree `<PS_P1B_LANDED_TREE>`. Use that landed implementation and permanent handoff `<PS_P1B_HANDOFF_URL>` as the T1B implementation source. Do not rebuild the writer, evidence protocol, lease/refspec checks, credential boundary, terminal result, or rollback design.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until this record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Record every PS source blob and every adapted Terraform destination blob. Change repository names, generated filenames, schema/type prefixes, check names, ruleset names, and local evidence identifiers. Preserve common permissions, job isolation, credential materialization, candidate consumption, ancestry/lease/refspec checks, native statuses, failure postconditions, cleanup, and audit truth.
>
> Use Terraform's own administrator issue and live settings evidence. Never copy PS ruleset IDs, request digests, application/rule responses, `main` values, or audit records. Refresh the reciprocal matrix at implementation start and before merge. An unexplained weaker behavior blocks merge.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Terraform #22 contains verified P1B source pins, scope, expected differences, settings boundary, and explicit start conditions.

### Complete when

Terraform #22 contains verified P1B source pins, scope, expected differences, settings boundary, and explicit start conditions.

## Task 194 — commence Terraform #22

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 193 completion record

~~~markdown
# Task 193 completion record

{{TASK_193_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 193 | `FS` | The issue record is complete, Terraform #21 is closed, and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #22. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only Terraform #22 from the landed PS P1B commit and final Terraform baseline.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 195 — implement Terraform #22

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 194 completion record

~~~markdown
# Task 194 completion record

{{TASK_194_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 194 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #22 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Adapt the writer and evidence protocol. Preserve the common controls named in Task 193.
2. Use only Terraform's authorized settings and live evidence.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 196 — create or update the candidate PR for Terraform #22

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 196 PR URL | `{{TASK_196_PR_URL}}` | Produced by this task; replace {{TASK_196_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 195 completion record

~~~markdown
# Task 195 completion record

{{TASK_195_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 195 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #22; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Complete the reciprocal matrix at candidate preparation. Treat unexplained weaker behavior as a blocker.
2. Create or update one focused Terraform #22 PR. Run initial validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #22 candidate PR exists with complete source, destination, validation, settings, and audit evidence.

### Complete when

The Terraform #22 candidate PR exists with complete source, destination, validation, settings, and audit evidence.

## Task 197 — run the Codex review loop on the Terraform #22 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 196 PR URL | `{{TASK_196_PR_URL}}` | Produced by Task 196; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 196 completion record

~~~markdown
# Task 196 completion record

{{TASK_196_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 196 | `FS` | The Terraform #22 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the Terraform #22 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below in TerraformStyleGuide. Do not merge or change settings without exact authorization. If bytes change, rerun Task 196 validation and refresh the reciprocal matrix.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 198 — run the independent final quality check on the Terraform #22 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 196 PR URL | `{{TASK_196_PR_URL}}` | Produced by Task 196; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 197 completion record

~~~markdown
# Task 197 completion record

{{TASK_197_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 197 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #22 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #22 issue, writer, settings, permissions, credentials, evidence, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 197 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 197.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 197.

## Task 199 — merge the Terraform #22 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 196 PR URL | `{{TASK_196_PR_URL}}` | Produced by Task 196; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 198 completion record

~~~markdown
# Task 198 completion record

{{TASK_198_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 198 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 200 — publish the landed handoff for the Terraform #22 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 196 PR URL | `{{TASK_196_PR_URL}}` | Produced by Task 196; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 199 completion record

~~~markdown
# Task 199 completion record

{{TASK_199_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 199 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

Terraform #22 is closed by the landed PR and its complete handoff is ready for reverse comparison.

### Complete when

Terraform #22 is closed by the landed PR and its complete handoff is ready for reverse comparison.

## Task 201 — reverse-compare T1B in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 200 completion record

~~~markdown
# Task 200 completion record

{{TASK_200_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 200 | `FS` | Use only the landed P1B and T1B commits and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 201 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 201 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 202 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 203 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 205 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 201 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 204 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 201 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 201 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 202 — create or update the focused repair issue for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 202 issue URL | `{{TASK_202_ISSUE_URL}}` | Produced by this task; replace {{TASK_202_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 201 completion record

~~~markdown
# Task 201 completion record

{{TASK_201_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 201 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 201's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 203 — commence the focused repair for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 202 issue URL | `{{TASK_202_ISSUE_URL}}` | Produced by Task 202; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 202 completion record

~~~markdown
# Task 202 completion record

{{TASK_202_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 202 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 202 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 204 — implement the focused repair for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 202 issue URL | `{{TASK_202_ISSUE_URL}}` | Produced by Task 202; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 203 completion record

~~~markdown
# Task 203 completion record

{{TASK_203_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 203 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 203 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 205 — create or update the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 202 issue URL | `{{TASK_202_ISSUE_URL}}` | Produced by Task 202; resolve globally before execution |
| Task 205 PR URL | `{{TASK_205_PR_URL}}` | Produced by this task; replace {{TASK_205_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 204 completion record

~~~markdown
# Task 204 completion record

{{TASK_204_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 204 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 204 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 206 — run the Codex review loop on the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 205 PR URL | `{{TASK_205_PR_URL}}` | Produced by Task 205; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 205 completion record

~~~markdown
# Task 205 completion record

{{TASK_205_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 205 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 205 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 205's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 206 for the new head before Task 207. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 207 — run the independent final quality check on the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 205 PR URL | `{{TASK_205_PR_URL}}` | Produced by Task 205; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 206 completion record

~~~markdown
# Task 206 completion record

{{TASK_206_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 206 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 206 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 205's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 206 for the new head, and then repeat Task 207 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 208 — merge the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 205 PR URL | `{{TASK_205_PR_URL}}` | Produced by Task 205; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 207 completion record

~~~markdown
# Task 207 completion record

{{TASK_207_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 207 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 207 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 209 — publish the landed repair handoff for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 205 PR URL | `{{TASK_205_PR_URL}}` | Produced by Task 205; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 208 completion record

~~~markdown
# Task 208 completion record

{{TASK_208_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 208 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 208 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 210 — perform the final Terraform T1B recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 209 completion record

~~~markdown
# Task 209 completion record

{{TASK_209_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 209 | `FS` | Use the final landed PS state from Task 209. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 210 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 210 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 211 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 213 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 220 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 220 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 211 — create or update the focused repair issue for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 211 issue URL | `{{TASK_211_ISSUE_URL}}` | Produced by this task; replace {{TASK_211_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 210 completion record

~~~markdown
# Task 210 completion record

{{TASK_210_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 210 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 210's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 212 — commence the focused repair for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 211 issue URL | `{{TASK_211_ISSUE_URL}}` | Produced by Task 211; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 211 completion record

~~~markdown
# Task 211 completion record

{{TASK_211_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 211 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 211 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 213 — implement the focused repair for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 211 issue URL | `{{TASK_211_ISSUE_URL}}` | Produced by Task 211; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 212 completion record

~~~markdown
# Task 212 completion record

{{TASK_212_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 212 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 212 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 214 — create or update the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 211 issue URL | `{{TASK_211_ISSUE_URL}}` | Produced by Task 211; resolve globally before execution |
| Task 214 PR URL | `{{TASK_214_PR_URL}}` | Produced by this task; replace {{TASK_214_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 213 completion record

~~~markdown
# Task 213 completion record

{{TASK_213_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 213 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 213 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 215 — run the Codex review loop on the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 214 PR URL | `{{TASK_214_PR_URL}}` | Produced by Task 214; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 214 completion record

~~~markdown
# Task 214 completion record

{{TASK_214_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 214 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 214 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 214's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 215 for the new head before Task 216. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 216 — run the independent final quality check on the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 214 PR URL | `{{TASK_214_PR_URL}}` | Produced by Task 214; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 215 completion record

~~~markdown
# Task 215 completion record

{{TASK_215_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 215 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 215 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 214's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 215 for the new head, and then repeat Task 216 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 217 — merge the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 214 PR URL | `{{TASK_214_PR_URL}}` | Produced by Task 214; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 216 completion record

~~~markdown
# Task 216 completion record

{{TASK_216_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 216 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 216 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 218 — publish the landed repair handoff for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 214 PR URL | `{{TASK_214_PR_URL}}` | Produced by Task 214; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 217 completion record

~~~markdown
# Task 217 completion record

{{TASK_217_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 217 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 217 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 219 — recheck the reciprocal result for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 210 completion record

~~~markdown
# Task 210 completion record

{{TASK_210_COMPLETION_RECORD}}
~~~

#### Task 218 completion record

~~~markdown
# Task 218 completion record

{{TASK_218_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 210 | `FS` | Use the original pinned comparison and branch result. |
| Task 218 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 204–218 as a new numbered-range instance. After its new handoff, repeat Task 219.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 220 — publish the fixed-point closure for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 219 completion record

~~~markdown
# Task 219 completion record

{{TASK_219_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 219 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 219 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 221 — commence PS #148

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 220 completion record

~~~markdown
# Task 220 completion record

{{TASK_220_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 220 | `FS` | The P1B/T1B cycle is closed and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement PS #148. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only PS #148.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 222 — implement PS #148

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 221 completion record

~~~markdown
# Task 221 completion record

{{TASK_221_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 221 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS #148 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #148 and its native dependency. Classify the capability and pin all landed inputs and affected paths.
2. Implement the issue contract.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 223 — create or update the candidate PR for PS #148

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 223 PR URL | `{{TASK_223_PR_URL}}` | Produced by this task; replace {{TASK_223_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 222 completion record

~~~markdown
# Task 222 completion record

{{TASK_222_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 222 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS #148; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one focused PS #148 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #148 candidate PR exists with all required identities, initial validation, and issue-contract evidence.

### Complete when

The PS #148 candidate PR exists with all required identities, initial validation, and issue-contract evidence.

## Task 224 — run the Codex review loop on the PS #148 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 223 PR URL | `{{TASK_223_PR_URL}}` | Produced by Task 223; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 223 completion record

~~~markdown
# Task 223 completion record

{{TASK_223_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 223 | `FS` | The PS #148 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the PS #148 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below against the PS #148 PR. Do not merge. If bytes change, rerun Task 223 validation.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 225 — run the independent final quality check on the PS #148 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 223 PR URL | `{{TASK_223_PR_URL}}` | Produced by Task 223; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 224 completion record

~~~markdown
# Task 224 completion record

{{TASK_224_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 224 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #148 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #148 issue-contract, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 224 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 224.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 224.

## Task 226 — merge the PS #148 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 223 PR URL | `{{TASK_223_PR_URL}}` | Produced by Task 223; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 225 completion record

~~~markdown
# Task 225 completion record

{{TASK_225_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 225 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 227 — publish the landed handoff for the PS #148 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 223 PR URL | `{{TASK_223_PR_URL}}` | Produced by Task 223; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 226 completion record

~~~markdown
# Task 226 completion record

{{TASK_226_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 226 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #148 is closed by the landed PR and its handoff is ready for Terraform comparison.

### Complete when

PS #148 is closed by the landed PR and its handoff is ready for Terraform comparison.

## Task 228 — compare PS #148 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 227 completion record

~~~markdown
# Task 227 completion record

{{TASK_227_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 227 | `FS` | Use the final landed PS #148 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 228 | Compare the affected PS #148 role with Terraform. |
| Task 229 | If common behavior is absent or weaker, create one focused Terraform issue from the landed PS handoff, then run the four task-local conditional repair PR lifecycle actions. |
| Task 228 | If Terraform is already equivalent, post a pinned no-change record. |
| Task 228 | If the blank-line example is PowerShell-specific, post a pinned non-applicability record. |
| Task 228 | Do not create an issue only for symmetry. |
| Task 228 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 228 | Map files by role, not only by filename. |
| Task 228 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 231 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 229 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 230 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 232 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 228 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 229 — create or update the focused repair issue for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 228 completion record

~~~markdown
# Task 228 completion record

{{TASK_228_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 228 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 228's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 230 — commence the focused repair for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 229 completion record

~~~markdown
# Task 229 completion record

{{TASK_229_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 229 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 229 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 231 — implement the focused repair for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 230 completion record

~~~markdown
# Task 230 completion record

{{TASK_230_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 230 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 230 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 232 — create or update the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 232 PR URL | `{{TASK_232_PR_URL}}` | Produced by this task; replace {{TASK_232_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 231 completion record

~~~markdown
# Task 231 completion record

{{TASK_231_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 231 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 231 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 233 — run the Codex review loop on the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 232 PR URL | `{{TASK_232_PR_URL}}` | Produced by Task 232; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 232 completion record

~~~markdown
# Task 232 completion record

{{TASK_232_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 232 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 232 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 232's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 233 for the new head before Task 234. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 234 — run the independent final quality check on the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 232 PR URL | `{{TASK_232_PR_URL}}` | Produced by Task 232; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 233 completion record

~~~markdown
# Task 233 completion record

{{TASK_233_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 233 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 233 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 232's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 233 for the new head, and then repeat Task 234 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 235 — merge the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 232 PR URL | `{{TASK_232_PR_URL}}` | Produced by Task 232; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 234 completion record

~~~markdown
# Task 234 completion record

{{TASK_234_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 234 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 234 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 236 — publish the landed repair handoff for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 232 PR URL | `{{TASK_232_PR_URL}}` | Produced by Task 232; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 235 completion record

~~~markdown
# Task 235 completion record

{{TASK_235_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 235 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 235 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 237 — reverse-compare the PS #148 cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 236 completion record

~~~markdown
# Task 236 completion record

{{TASK_236_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 236 | `FS` | Use the final landed or no-change result from Task 236. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 237 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 237 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 238 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 239 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 241 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 246 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 240 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 247 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 247 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 238 — create or update the focused repair issue for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 237 completion record

~~~markdown
# Task 237 completion record

{{TASK_237_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 237 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 237's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 239 — commence the focused repair for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 238 completion record

~~~markdown
# Task 238 completion record

{{TASK_238_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 238 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 238 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 240 — implement the focused repair for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 239 completion record

~~~markdown
# Task 239 completion record

{{TASK_239_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 239 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 239 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 241 — create or update the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 241 PR URL | `{{TASK_241_PR_URL}}` | Produced by this task; replace {{TASK_241_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 240 completion record

~~~markdown
# Task 240 completion record

{{TASK_240_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 240 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 240 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 242 — run the Codex review loop on the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 241 PR URL | `{{TASK_241_PR_URL}}` | Produced by Task 241; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 241 completion record

~~~markdown
# Task 241 completion record

{{TASK_241_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 241 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 241 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 241's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 242 for the new head before Task 243. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 243 — run the independent final quality check on the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 241 PR URL | `{{TASK_241_PR_URL}}` | Produced by Task 241; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 242 completion record

~~~markdown
# Task 242 completion record

{{TASK_242_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 242 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 242 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 241's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 242 for the new head, and then repeat Task 243 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 244 — merge the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 241 PR URL | `{{TASK_241_PR_URL}}` | Produced by Task 241; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 243 completion record

~~~markdown
# Task 243 completion record

{{TASK_243_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 243 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 243 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 245 — publish the landed repair handoff for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 241 PR URL | `{{TASK_241_PR_URL}}` | Produced by Task 241; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 244 completion record

~~~markdown
# Task 244 completion record

{{TASK_244_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 244 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 244 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 246 — recheck the reciprocal result for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 237 completion record

~~~markdown
# Task 237 completion record

{{TASK_237_COMPLETION_RECORD}}
~~~

#### Task 245 completion record

~~~markdown
# Task 245 completion record

{{TASK_245_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 237 | `FS` | Use the original pinned comparison and branch result. |
| Task 245 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 231–245 as a new numbered-range instance. After its new handoff, repeat Task 246.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 247 — publish the fixed-point closure for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 246 completion record

~~~markdown
# Task 246 completion record

{{TASK_246_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 246 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 246 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 248 — commence Terraform #23

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 200 completion record

~~~markdown
# Task 200 completion record

{{TASK_200_COMPLETION_RECORD}}
~~~

#### Task 247 completion record

~~~markdown
# Task 247 completion record

{{TASK_247_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 247 | `FS` | The PS #148 cycle is closed and the implementation slot is free. |
| Task 200 | `FS` | Terraform #22, the native predecessor of #23, is closed. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #23. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only Terraform #23.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 249 — implement Terraform #23

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 248 completion record

~~~markdown
# Task 248 completion record

{{TASK_248_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 248 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #23 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query Terraform #23 and verify its native prerequisites.
2. Classify it as Terraform-specific unless comparison proves a common PS role.
3. Implement its state-version recovery contract.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 250 — create or update the candidate PR for Terraform #23

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 250 PR URL | `{{TASK_250_PR_URL}}` | Produced by this task; replace {{TASK_250_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 249 completion record

~~~markdown
# Task 249 completion record

{{TASK_249_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 249 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #23; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one focused Terraform #23 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #23 candidate PR exists with complete initial identity, validation, failure, recovery, and evidence records.

### Complete when

The Terraform #23 candidate PR exists with complete initial identity, validation, failure, recovery, and evidence records.

## Task 251 — run the Codex review loop on the Terraform #23 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 250 PR URL | `{{TASK_250_PR_URL}}` | Produced by Task 250; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 250 completion record

~~~markdown
# Task 250 completion record

{{TASK_250_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 250 | `FS` | The Terraform #23 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the Terraform #23 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below in TerraformStyleGuide. Do not merge. If bytes change, rerun Task 250 validation.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 252 — run the independent final quality check on the Terraform #23 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 250 PR URL | `{{TASK_250_PR_URL}}` | Produced by Task 250; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 251 completion record

~~~markdown
# Task 251 completion record

{{TASK_251_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 251 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #23 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #23 state-version recovery, issue, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 251 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 251.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 251.

## Task 253 — merge the Terraform #23 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 250 PR URL | `{{TASK_250_PR_URL}}` | Produced by Task 250; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 252 completion record

~~~markdown
# Task 252 completion record

{{TASK_252_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 252 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 254 — publish the landed handoff for the Terraform #23 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 250 PR URL | `{{TASK_250_PR_URL}}` | Produced by Task 250; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 253 completion record

~~~markdown
# Task 253 completion record

{{TASK_253_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 253 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

Terraform #23 is closed by the landed PR and its handoff is ready for PS applicability review.

### Complete when

Terraform #23 is closed by the landed PR and its handoff is ready for PS applicability review.

## Task 255 — evaluate Terraform #23 applicability in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 254 completion record

~~~markdown
# Task 254 completion record

{{TASK_254_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 254 | `FS` | Use the landed Terraform #23 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 255 | Search PS for a role affected by Terraform #23's state-version recovery behavior. |
| Task 256 | If PS has a common role and is absent or weaker, create one focused PS issue, then use the task-local execution controls, conditional repair PR lifecycle, and reciprocal fixed-point controls below. |
| Task 255 | If PS is already equivalent, post a pinned no-change record. |
| Task 255 | If PS has no corresponding role, post a pinned non-applicability record. |
| Task 255 | Do not invent a PS issue only for symmetry. |
| Task 255 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 255 | Map files by role, not only by filename. |
| Task 255 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 258 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 256 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 257 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 259 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 264 | Compare the landed destination result back against the source repository. |
| Task 256 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 259 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 258 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 265 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 265 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 256 — create or update the focused repair issue for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 255 completion record

~~~markdown
# Task 255 completion record

{{TASK_255_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 255 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 255's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 257 — commence the focused repair for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 256 completion record

~~~markdown
# Task 256 completion record

{{TASK_256_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 256 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 256 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 258 — implement the focused repair for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 257 completion record

~~~markdown
# Task 257 completion record

{{TASK_257_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 257 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 257 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 259 — create or update the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 259 PR URL | `{{TASK_259_PR_URL}}` | Produced by this task; replace {{TASK_259_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 258 completion record

~~~markdown
# Task 258 completion record

{{TASK_258_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 258 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 258 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 260 — run the Codex review loop on the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 259 PR URL | `{{TASK_259_PR_URL}}` | Produced by Task 259; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 259 completion record

~~~markdown
# Task 259 completion record

{{TASK_259_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 259 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 259 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 259's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 260 for the new head before Task 261. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 261 — run the independent final quality check on the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 259 PR URL | `{{TASK_259_PR_URL}}` | Produced by Task 259; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 260 completion record

~~~markdown
# Task 260 completion record

{{TASK_260_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 260 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 260 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 259's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 260 for the new head, and then repeat Task 261 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 262 — merge the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 259 PR URL | `{{TASK_259_PR_URL}}` | Produced by Task 259; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 261 completion record

~~~markdown
# Task 261 completion record

{{TASK_261_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 261 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 261 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 263 — publish the landed repair handoff for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 259 PR URL | `{{TASK_259_PR_URL}}` | Produced by Task 259; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 262 completion record

~~~markdown
# Task 262 completion record

{{TASK_262_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 262 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 262 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 264 — recheck the reciprocal result for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 255 completion record

~~~markdown
# Task 255 completion record

{{TASK_255_COMPLETION_RECORD}}
~~~

#### Task 263 completion record

~~~markdown
# Task 263 completion record

{{TASK_263_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 255 | `FS` | Use the original pinned comparison and branch result. |
| Task 263 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 249–263 as a new numbered-range instance. After its new handoff, repeat Task 264.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 265 — publish the fixed-point closure for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 264 completion record

~~~markdown
# Task 264 completion record

{{TASK_264_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 264 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 264 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 266 — commence the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 192 completion record

~~~markdown
# Task 192 completion record

{{TASK_192_COMPLETION_RECORD}}
~~~

#### Task 227 completion record

~~~markdown
# Task 227 completion record

{{TASK_227_COMPLETION_RECORD}}
~~~

#### Task 265 completion record

~~~markdown
# Task 265 completion record

{{TASK_265_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 265 | `FS` | Terraform #23 and its PS applicability cycle are complete. |
| Task 192 | `FS` | The P1B PS handoff exists. |
| Task 227 | `FS` | The P2 PS handoff exists. |
| Completed PS #145 and #146 | `FS` | The P1 and P1A handoffs exist. |

### Objective

Explicitly commence only the implementation governed by prepare the PS P3 candidate for merge. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Verify the implementation slot is free. Explicitly commence only PS #149.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 267 — prepare the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 266 completion record

~~~markdown
# Task 266 completion record

{{TASK_266_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 266 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate prepare the PS P3 candidate for merge without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Verify the P1, P1A, P1B, and P2 landed handoffs and all native issue dependencies.
2. Re-query PS #149, Terraform #24, and current dependency, package-manager, audit, process-runner, exception, Husky, workflow, and evidence contracts.
3. Pin every source commit and blob. Record intended paths and expected repository-specific differences.
4. Implement the complete PS #149 contract. Preserve common isolation, exact toolchain selection, install/audit vectors, configuration neutralization, bounded process I/O, termination states, JSON validation, exception governance, live-issue verification, scheduled/manual read-only behavior, failure truth, and evidence retention.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 268 — create or update the candidate PR for the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 268 PR URL | `{{TASK_268_PR_URL}}` | Produced by this task; replace {{TASK_268_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 267 completion record

~~~markdown
# Task 267 completion record

{{TASK_267_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 267 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for prepare the PS P3 candidate for merge; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Create or update one PS #149 PR. Reach one locally validated candidate head. Record its base, head/tree, checks, paths and blobs, validation, runtime identities, decision-record changes, and rollback inputs.
2. Stop before review-loop execution or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #149 has one locally validated candidate PR and a verified evidence package.

### Complete when

PS #149 has one locally validated candidate PR and a verified evidence package.

## Task 269 — run the Codex review loop on the PS #149 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 268 PR URL | `{{TASK_268_PR_URL}}` | Produced by Task 268; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 268 completion record

~~~markdown
# Task 268 completion record

{{TASK_268_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 268 | `FS` | The PS #149 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the PS #149 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below against the PS #149 PR. Do not merge. If bytes change, rerun Task 268 validation and refresh its evidence package.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 270 — run the independent final quality check on the PS #149 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 268 PR URL | `{{TASK_268_PR_URL}}` | Produced by Task 268; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 269 completion record

~~~markdown
# Task 269 completion record

{{TASK_269_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 269 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #149 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #149 governance, dependency, audit, Husky, exception, scheduled/manual behavior, issue, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 269 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 269. The PR is eligible for the checkpoint decision but remains unmerged.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 269. The PR is eligible for the checkpoint decision but remains unmerged.

## Task 271 — check the PS #149 advisory-deadline state

> **Execution class: Coding agent executable.** Run this task at exactly `2026-08-21T23:59:59Z`. It is a status check, not implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 268 PR URL | `{{TASK_268_PR_URL}}` | Produced by Task 268; resolve globally before execution |

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| None | Calendar constraint | Run at `2026-08-21T23:59:59Z` even if an earlier numbered task is incomplete. |

The PS P1 advisory decision expires at `2026-08-30T23:59:59Z`. The checkpoint is a coordination gate, not a native issue dependency.

### Objective

Complete only this leaf action: check the PS #149 advisory-deadline state. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #149 and the P1, P1A, P1B, and P2 handoffs.
2. Record each issue and PR state, reviewed head, checks, dependency state, and merge readiness.
3. If PS #149 is merge-ready, record that its current merge path is available for Task 272.
4. If PS #149 is not merge-ready, prepare the fallback evidence package for Task 272.
5. Record incomplete earlier tasks explicitly. Do not wait beyond the checkpoint and do not create a false PS #149 dependency.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

One timestamped, verified checkpoint record states whether the PS #149 merge path is ready and contains the evidence needed for the owner decision.

### Complete when

One timestamped, verified checkpoint record states whether the PS #149 merge path is ready and contains the evidence needed for the owner decision.

## Task 272 — select and approve the PS #149 deadline path

> **Execution class: Human execution required.** The repository owner must select the deadline path.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 268 PR URL | `{{TASK_268_PR_URL}}` | Produced by Task 268; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 271 completion record

~~~markdown
# Task 271 completion record

{{TASK_271_COMPLETION_RECORD}}
~~~

#### Task 272 authorization record

~~~markdown
# Task 272 authorization record

{{TASK_272_AUTHORIZATION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 271 | `FS` | Use the exact timestamped checkpoint evidence. |

### Objective

Complete only this leaf action: select and approve the PS #149 deadline path. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Human actions:**

1. If PS #149 is merge-ready, approve or reject its existing merge path.
2. If PS #149 is not merge-ready, approve or reject a separate superseding-decision task.
3. If the superseding task is approved, set its start date to `2026-08-22`.
4. Record the approved scope, owner, canonical approval, and expiry.
5. Approve every repository-settings change in the applicable administrator issue.
6. Do not extend the expiry without a new recorded decision.
7. Do not reuse approval for different bytes or settings.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The canonical record approves the current PS #149 merge path or a separate fallback, or explicitly rejects both. If it rejects both, stop the affected sequence and obtain a new owner decision before Task 283.

### Complete when

The canonical record approves the current PS #149 merge path or a separate fallback, or explicitly rejects both. If it rejects both, stop the affected sequence and obtain a new owner decision before Task 283.

## Task 273 — create or update the approved advisory-fallback issue

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 272 completion record

~~~markdown
# Task 272 completion record

{{TASK_272_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 272 | `FS` | The owner approved a separate superseding-decision task and its exact scope. |

### Objective

Create or update only the focused advisory-fallback issue authorized by the owner. Do not commence implementation. If no fallback was approved, record `SKIPPED — EXISTING MERGE PATH` and do not create an issue.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the Task-local owner decision, PS #149, its current PR, the advisory evidence, and all repository dependencies.
2. If the owner approved the existing PS #149 merge path and did not approve a fallback, record `SKIPPED — EXISTING MERGE PATH` and stop.
3. If a fallback was approved, search for the separately approved focused issue. Update it when it already represents the exact scope; otherwise create it.
4. Record the canonical approval, owner, start date, expiry, exact fallback scope, affected paths, replacement contract, validator/case requirements, compensating controls, validation, rollback, and real native dependencies.
5. Read the issue and dependencies back. Do not create a branch, edit bytes, update a PR, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The exact approved fallback issue exists without implementation commencement, or the task is recorded `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The exact approved fallback issue exists without implementation commencement, or the task is recorded `SKIPPED — EXISTING MERGE PATH`.

## Task 274 — commence an approved advisory fallback

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 273 completion record

~~~markdown
# Task 273 completion record

{{TASK_273_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 273 | `FS` | The issue update is complete and provides the starting point. |

### Objective

Explicitly commence only the implementation governed by implement an approved advisory fallback. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 273 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Re-query the approved fallback issue, exact owner authorization, native dependencies, both current main refs, and active implementation PR inventory.
3. Require the approved start date to have arrived, every prerequisite to be complete, and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, approval, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and validation plan. Do not edit or update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 275 — implement an approved advisory fallback

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 274 completion record

~~~markdown
# Task 274 completion record

{{TASK_274_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 274 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement an approved advisory fallback without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 274 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Verify that no feature implementation issue or PR is active. If the PS #149 PR is still active, preserve its exact head and tree. Close it only when the Task 272 approval expressly authorizes closure. Otherwise, do not start fallback implementation.
3. Prepare the complete fallback from the approved scope.
4. Include the current audit, every accepted finding, each reason, and each compensating control.
5. Include the canonical approval and expiry, exact replacement contract bytes, validator and case changes, and every dependent version, digest, and name pin.
6. Include validation and rollback instructions.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 276 — create or update the candidate PR for an approved advisory fallback

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 276 PR URL | `{{TASK_276_PR_URL}}` | Produced by this task; replace {{TASK_276_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 275 completion record

~~~markdown
# Task 275 completion record

{{TASK_275_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 275 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement an approved advisory fallback; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 275 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Create one focused fallback PR under the separately approved task. Run initial local validation and required checks. Record its head SHA and tree.
3. Prove on the candidate that the validator does not enter `advisory-expired`. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The approved fallback candidate PR exists with all required decision, identity, initial validation, and rollback evidence. If the existing merge path was approved instead, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The approved fallback candidate PR exists with all required decision, identity, initial validation, and rollback evidence. If the existing merge path was approved instead, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

## Task 277 — run the Codex review loop on the fallback PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).** Skip this task when Task 276 is skipped.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 276 PR URL | `{{TASK_276_PR_URL}}` | Produced by Task 276; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 276 completion record

~~~markdown
# Task 276 completion record

{{TASK_276_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 276 | `FS` | The approved fallback candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the fallback PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below against the fallback PR. Do not merge. If bytes change, rerun Task 276 validation and expiry proof.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 278 — run the independent final quality check on the fallback PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session. Skip this task when Task 276 is skipped.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 276 PR URL | `{{TASK_276_PR_URL}}` | Produced by Task 276; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 277 completion record

~~~markdown
# Task 277 completion record

{{TASK_277_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 277 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the fallback PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require exact approval/scope, advisory-expiry, contract, validator, case, pin, rollback, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 277 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 277.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 277.

## Task 279 — merge the approved fallback PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 276 PR URL | `{{TASK_276_PR_URL}}` | Produced by Task 276; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 278 completion record

~~~markdown
# Task 278 completion record

{{TASK_278_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 278 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If the approved fallback path was not selected and the predecessor review/quality tasks were skipped, record `SKIPPED — EXISTING MERGE PATH` and stop.
2. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
4. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
5. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 280 — publish the landed handoff for the approved fallback PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 276 PR URL | `{{TASK_276_PR_URL}}` | Produced by Task 276; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 279 completion record

~~~markdown
# Task 279 completion record

{{TASK_279_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 279 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If the predecessor recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
4. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The approved fallback is landed with all required evidence before advisory expiry. If the fallback was not selected, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The approved fallback is landed with all required evidence before advisory expiry. If the fallback was not selected, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

## Task 281 — check whether the PS #149 candidate requires a post-fallback refresh

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 268 PR URL | `{{TASK_268_PR_URL}}` | Produced by Task 268; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 270 completion record

~~~markdown
# Task 270 completion record

{{TASK_270_COMPLETION_RECORD}}
~~~

#### Task 272 completion record

~~~markdown
# Task 272 completion record

{{TASK_272_COMPLETION_RECORD}}
~~~

#### Task 280 completion record

~~~markdown
# Task 280 completion record

{{TASK_280_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 270 | `FS` | The PS #149 candidate passed review and the independent quality check. |
| Task 272 | `FS` | The owner approved the current merge path or supplied a later applicable approval. |
| Task 280 | `FS` when applicable | The approved fallback is landed. |

### Objective

Decide whether the reviewed PS #149 candidate remains merge-ready after the approved fallback path. Do not edit, update a PR, request review, approve, merge, or publish a handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. If Task 280 changed an input or overlapping path, rebase or update the PS #149 candidate. Return to Task 269 for the Codex review loop and Task 270 for a new independent quality check. Obtain a new Task 272 approval when the prior approval does not cover the resulting bytes.

### Procedure

1. Re-query the PS #149 candidate, the fallback branch result, all dependencies, decisions, settings, approvals, reviews, checks, and current `main`.
2. Compare the reviewed candidate head/tree and every input or overlapping path with the state after the fallback branch.
3. If no relevant byte, input, setting, approval, or evidence changed, output `MERGE_READY` for the exact current PS #149 head/tree.
4. If refresh is required, output `REFRESH_REQUIRED` with the exact scope. Repeat Tasks 267 and 265 as new update instances, then repeat Tasks 269 and 267. Repeat Task 272 when the prior approval does not cover the resulting bytes or settings. Then repeat this task.
5. Do not perform any refresh action in this task.

### Candidate-refresh gate

Apply this gate only if Procedure step 2 changes the PS #149 PR:

1. Complete the affected issue scope, local validation, PR-body correction, and initial required checks on the new candidate.
2. Record the new base, head SHA and tree, affected paths, issue links, validation, runtime identities, and changed decision or rollback evidence.
3. Stop the merge action. Return to Task 269 for a new Codex review loop and Task 270 for a new independent final quality check.
4. Require a new Task 272 approval when the current approval does not cover the new bytes or settings. Do not resume this task until all three records apply to the same current head and tree.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The current PS #149 candidate has an exact `MERGE_READY` record, or this instance emits `REFRESH_REQUIRED` with the complete repeat path.

### Complete when

The current PS #149 candidate has an exact `MERGE_READY` record, or this instance emits `REFRESH_REQUIRED` with the complete repeat path.

## Task 282 — merge the PS #149 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 268 PR URL | `{{TASK_268_PR_URL}}` | Produced by Task 268; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 281 completion record

~~~markdown
# Task 281 completion record

{{TASK_281_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 281 | `FS` | Require `MERGE_READY` for the exact current PS #149 head/tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Revalidate the approved candidate head, tree, checks, dependencies, decisions, settings, and approval.
2. Stop if any item still differs from the approved evidence.
3. Apply the task-local merge gate below.
4. Merge one PS #149 PR and close the issue.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 283 — publish the landed handoff for the PS #149 P3 implementation

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 268 PR URL | `{{TASK_268_PR_URL}}` | Produced by Task 268; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 282 completion record

~~~markdown
# Task 282 completion record

{{TASK_282_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 282 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Post the permanent P3 handoff. Include source and landed identities, paths and blobs, common mechanism choice, package and tool identities, validation, runtime evidence, decision records, review-loop result, final quality-check result, intentional differences, and rollback instructions.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #149 is closed by the approved merge and Terraform #24 can adapt the landed result without redesign.

### Complete when

PS #149 is closed by the approved merge and Terraform #24 can adapt the landed result without redesign.

## Task 284 — update Terraform #24 from the landed PS P3 handoff

> **Execution class: Coding agent executable.** This is issue setup and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 254 completion record

~~~markdown
# Task 254 completion record

{{TASK_254_COMPLETION_RECORD}}
~~~

#### Task 283 completion record

~~~markdown
# Task 283 completion record

{{TASK_283_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 283 | `FS` | Use the final landed PS #149 commit and handoff. |
| Task 254 | `FS` | Terraform #23, the native predecessor of #24, is closed. |

### Objective

Complete only this leaf action: update Terraform #24 from the landed PS P3 handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Issue update:**

Replace all placeholders with verified identities and update Terraform #24:

> PSStyleGuide dependency and npm governance landed through `<PS_P3_PR_URL>` at `<PS_P3_LANDED_COMMIT>`, tree `<PS_P3_LANDED_TREE>`. Use permanent handoff `<PS_P3_HANDOFF_URL>` as the implementation source.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until this record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read every relevant PS source blob from that commit. Do not independently select a package-manager policy, audit parser, process runner, exception schema, Husky installer, workflow topology, or evidence format. Adapt repository identity, issue URLs, package findings, generated filenames, check names, and Terraform-specific dependency graph.
>
> Preserve common isolation, exact toolchain selection, install/audit vectors, configuration neutralization, bounded process I/O, termination states, JSON validation, exception governance, live-issue verification, scheduled/manual read-only behavior, and failure truth. Refresh the reciprocal matrix at start and before merge. An unexplained weaker behavior blocks merge.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Terraform #24 contains the complete pinned starting point, scope, common controls, allowed adaptation surface, and start conditions.

### Complete when

Terraform #24 contains the complete pinned starting point, scope, common controls, allowed adaptation surface, and start conditions.

## Task 285 — commence Terraform #24

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 284 completion record

~~~markdown
# Task 284 completion record

{{TASK_284_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 284 | `FS` | The issue update is complete and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #24. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only Terraform #24 from the landed PS P3 source.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 286 — implement Terraform #24

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 285 completion record

~~~markdown
# Task 285 completion record

{{TASK_285_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 285 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #24 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Adapt the capability; do not independently redesign the named contracts.
2. Preserve all common controls from Task 284 and record every repository-specific adaptation.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 287 — create or update the candidate PR for Terraform #24

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 287 PR URL | `{{TASK_287_PR_URL}}` | Produced by this task; replace {{TASK_287_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 286 completion record

~~~markdown
# Task 286 completion record

{{TASK_286_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 286 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #24; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Complete the reciprocal matrix at candidate preparation.
2. Create or update one focused Terraform #24 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #24 candidate PR exists with complete source, destination, identity, initial validation, runtime, and intentional-difference evidence.

### Complete when

The Terraform #24 candidate PR exists with complete source, destination, identity, initial validation, runtime, and intentional-difference evidence.

## Task 288 — run the Codex review loop on the Terraform #24 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 287 PR URL | `{{TASK_287_PR_URL}}` | Produced by Task 287; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 287 completion record

~~~markdown
# Task 287 completion record

{{TASK_287_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 287 | `FS` | The Terraform #24 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the Terraform #24 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below in TerraformStyleGuide. Do not merge. If bytes change, rerun Task 287 validation and refresh the reciprocal matrix.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 289 — run the independent final quality check on the Terraform #24 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 287 PR URL | `{{TASK_287_PR_URL}}` | Produced by Task 287; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 288 completion record

~~~markdown
# Task 288 completion record

{{TASK_288_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 288 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #24 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #24 package, audit, exception, Husky, workflow, issue, comment, deferral, PR-body, validation, checks, and mergeability coverage. Return to Task 288 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 288.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 288.

## Task 290 — merge the Terraform #24 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 287 PR URL | `{{TASK_287_PR_URL}}` | Produced by Task 287; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 289 completion record

~~~markdown
# Task 289 completion record

{{TASK_289_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 289 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 291 — publish the landed handoff for the Terraform #24 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 287 PR URL | `{{TASK_287_PR_URL}}` | Produced by Task 287; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 290 completion record

~~~markdown
# Task 290 completion record

{{TASK_290_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 290 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

Terraform #24 is closed by the landed PR and the result is ready for reverse PS comparison.

### Complete when

Terraform #24 is closed by the landed PR and the result is ready for reverse PS comparison.

## Task 292 — reverse-compare T3 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 291 completion record

~~~markdown
# Task 291 completion record

{{TASK_291_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 291 | `FS` | Use only the landed PS #149 and Terraform #24 commits and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 292 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 292 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 293 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 294 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 296 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 292 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 295 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 292 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 292 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 293 — create or update the focused repair issue for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 293 issue URL | `{{TASK_293_ISSUE_URL}}` | Produced by this task; replace {{TASK_293_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 292 completion record

~~~markdown
# Task 292 completion record

{{TASK_292_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 292 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 292's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 294 — commence the focused repair for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 293 issue URL | `{{TASK_293_ISSUE_URL}}` | Produced by Task 293; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 293 completion record

~~~markdown
# Task 293 completion record

{{TASK_293_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 293 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 293 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 295 — implement the focused repair for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 293 issue URL | `{{TASK_293_ISSUE_URL}}` | Produced by Task 293; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 294 completion record

~~~markdown
# Task 294 completion record

{{TASK_294_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 294 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 294 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 296 — create or update the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 293 issue URL | `{{TASK_293_ISSUE_URL}}` | Produced by Task 293; resolve globally before execution |
| Task 296 PR URL | `{{TASK_296_PR_URL}}` | Produced by this task; replace {{TASK_296_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 295 completion record

~~~markdown
# Task 295 completion record

{{TASK_295_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 295 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 295 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 297 — run the Codex review loop on the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 296 PR URL | `{{TASK_296_PR_URL}}` | Produced by Task 296; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 296 completion record

~~~markdown
# Task 296 completion record

{{TASK_296_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 296 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 296 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 296's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 297 for the new head before Task 298. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 298 — run the independent final quality check on the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 296 PR URL | `{{TASK_296_PR_URL}}` | Produced by Task 296; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 297 completion record

~~~markdown
# Task 297 completion record

{{TASK_297_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 297 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 297 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 296's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 297 for the new head, and then repeat Task 298 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 299 — merge the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 296 PR URL | `{{TASK_296_PR_URL}}` | Produced by Task 296; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 298 completion record

~~~markdown
# Task 298 completion record

{{TASK_298_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 298 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 298 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 300 — publish the landed repair handoff for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 296 PR URL | `{{TASK_296_PR_URL}}` | Produced by Task 296; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 299 completion record

~~~markdown
# Task 299 completion record

{{TASK_299_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 299 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 299 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 301 — perform the final Terraform T3 recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 300 completion record

~~~markdown
# Task 300 completion record

{{TASK_300_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 300 | `FS` | Use the final landed PS state from Task 300. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 301 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 301 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 302 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 304 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 311 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 311 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 302 — create or update the focused repair issue for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 302 issue URL | `{{TASK_302_ISSUE_URL}}` | Produced by this task; replace {{TASK_302_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 301 completion record

~~~markdown
# Task 301 completion record

{{TASK_301_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 301 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 301's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 303 — commence the focused repair for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 302 issue URL | `{{TASK_302_ISSUE_URL}}` | Produced by Task 302; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 302 completion record

~~~markdown
# Task 302 completion record

{{TASK_302_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 302 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 302 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 304 — implement the focused repair for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 302 issue URL | `{{TASK_302_ISSUE_URL}}` | Produced by Task 302; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 303 completion record

~~~markdown
# Task 303 completion record

{{TASK_303_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 303 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 303 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 305 — create or update the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 302 issue URL | `{{TASK_302_ISSUE_URL}}` | Produced by Task 302; resolve globally before execution |
| Task 305 PR URL | `{{TASK_305_PR_URL}}` | Produced by this task; replace {{TASK_305_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 304 completion record

~~~markdown
# Task 304 completion record

{{TASK_304_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 304 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 304 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 306 — run the Codex review loop on the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 305 PR URL | `{{TASK_305_PR_URL}}` | Produced by Task 305; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 305 completion record

~~~markdown
# Task 305 completion record

{{TASK_305_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 305 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 305 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 305's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 306 for the new head before Task 307. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 307 — run the independent final quality check on the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 305 PR URL | `{{TASK_305_PR_URL}}` | Produced by Task 305; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 306 completion record

~~~markdown
# Task 306 completion record

{{TASK_306_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 306 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 306 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 305's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 306 for the new head, and then repeat Task 307 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 308 — merge the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 305 PR URL | `{{TASK_305_PR_URL}}` | Produced by Task 305; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 307 completion record

~~~markdown
# Task 307 completion record

{{TASK_307_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 307 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 307 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 309 — publish the landed repair handoff for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 305 PR URL | `{{TASK_305_PR_URL}}` | Produced by Task 305; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 308 completion record

~~~markdown
# Task 308 completion record

{{TASK_308_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 308 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 308 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 310 — recheck the reciprocal result for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 301 completion record

~~~markdown
# Task 301 completion record

{{TASK_301_COMPLETION_RECORD}}
~~~

#### Task 309 completion record

~~~markdown
# Task 309 completion record

{{TASK_309_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 301 | `FS` | Use the original pinned comparison and branch result. |
| Task 309 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 295–309 as a new numbered-range instance. After its new handoff, repeat Task 310.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 311 — publish the fixed-point closure for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 310 completion record

~~~markdown
# Task 310 completion record

{{TASK_310_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 310 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 310 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 312 — commence PS #151

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 311 completion record

~~~markdown
# Task 311 completion record

{{TASK_311_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 311 | `FS` | PS #149 and its full reciprocal cycle are closed. |

### Objective

Explicitly commence only the implementation governed by implement PS #151. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Verify the implementation slot is free. Explicitly commence only PS #151.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 313 — implement PS #151

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 312 completion record

~~~markdown
# Task 312 completion record

{{TASK_312_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 312 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS #151 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #151 and all landed policy and P3 contracts.
2. Implement the issue's Node permission-model confinement and duplicate-key preflight controls without silently attributing them to PS #162.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 314 — create or update the candidate PR for PS #151

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 314 PR URL | `{{TASK_314_PR_URL}}` | Produced by this task; replace {{TASK_314_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 313 completion record

~~~markdown
# Task 313 completion record

{{TASK_313_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 313 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS #151; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one focused PS #151 PR. Run initial local validation and required checks. Record its head SHA and tree, affected controls, files and blobs, runtimes, and intentional differences. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #151 candidate PR exists with a complete affected-control map and initial validation.

### Complete when

The PS #151 candidate PR exists with a complete affected-control map and initial validation.

## Task 315 — run the Codex review loop on the PS #151 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 314 PR URL | `{{TASK_314_PR_URL}}` | Produced by Task 314; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 314 completion record

~~~markdown
# Task 314 completion record

{{TASK_314_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 314 | `FS` | The PS #151 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Codex review loop on the PS #151 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Codex review-loop prompt below against the PS #151 PR. Do not merge. If bytes change, rerun Task 314 validation.

### Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 316 — run the independent final quality check on the PS #151 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 314 PR URL | `{{TASK_314_PR_URL}}` | Produced by Task 314; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 315 completion record

~~~markdown
# Task 315 completion record

{{TASK_315_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 315 | `FS` | The Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #151 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #151 Node permission-model, duplicate-key preflight, issue, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 315 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 315.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 315.

## Task 317 — merge the PS #151 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 314 PR URL | `{{TASK_314_PR_URL}}` | Produced by Task 314; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 316 completion record

~~~markdown
# Task 316 completion record

{{TASK_316_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 316 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 318 — publish the landed handoff for the PS #151 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 314 PR URL | `{{TASK_314_PR_URL}}` | Produced by Task 314; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 317 completion record

~~~markdown
# Task 317 completion record

{{TASK_317_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 317 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #151 is closed by the landed PR and its affected-control map is ready for Terraform comparison.

### Complete when

PS #151 is closed by the landed PR and its affected-control map is ready for Terraform comparison.

## Task 319 — compare PS #151 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 318 completion record

~~~markdown
# Task 318 completion record

{{TASK_318_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 318 | `FS` | Use the final landed PS #151 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 319 | Compare the landed Node permission-model and duplicate-key controls with the Terraform validator and workflow topology. |
| Task 320 | If an applicable control is absent or weaker, create one focused Terraform issue from the landed PS commit and blobs, then run the four task-local conditional repair PR lifecycle actions. |
| Task 319 | If Terraform is equivalent, post a pinned no-change record. |
| Task 319 | If a control is structurally inapplicable, post a pinned non-applicability record. |
| Task 319 | Do not create a placeholder issue only for symmetry. |
| Task 319 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 319 | Map files by role, not only by filename. |
| Task 319 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 322 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 320 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 321 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 323 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 319 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 320 — create or update the focused repair issue for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 319 completion record

~~~markdown
# Task 319 completion record

{{TASK_319_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 319 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 319's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 321 — commence the focused repair for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 320 completion record

~~~markdown
# Task 320 completion record

{{TASK_320_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 320 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 320 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 322 — implement the focused repair for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 321 completion record

~~~markdown
# Task 321 completion record

{{TASK_321_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 321 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 321 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 323 — create or update the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 323 PR URL | `{{TASK_323_PR_URL}}` | Produced by this task; replace {{TASK_323_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 322 completion record

~~~markdown
# Task 322 completion record

{{TASK_322_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 322 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 322 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 324 — run the Codex review loop on the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 323 PR URL | `{{TASK_323_PR_URL}}` | Produced by Task 323; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 323 completion record

~~~markdown
# Task 323 completion record

{{TASK_323_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 323 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 323 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 323's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 324 for the new head before Task 325. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 325 — run the independent final quality check on the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 323 PR URL | `{{TASK_323_PR_URL}}` | Produced by Task 323; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 324 completion record

~~~markdown
# Task 324 completion record

{{TASK_324_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 324 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 324 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 323's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 324 for the new head, and then repeat Task 325 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 326 — merge the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 323 PR URL | `{{TASK_323_PR_URL}}` | Produced by Task 323; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 325 completion record

~~~markdown
# Task 325 completion record

{{TASK_325_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 325 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 325 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 327 — publish the landed repair handoff for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 323 PR URL | `{{TASK_323_PR_URL}}` | Produced by Task 323; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 326 completion record

~~~markdown
# Task 326 completion record

{{TASK_326_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 326 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 326 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 328 — reverse-compare the PS #151 cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 327 completion record

~~~markdown
# Task 327 completion record

{{TASK_327_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 327 | `FS` | Use the final Task 327 Terraform result. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 328 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 328 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 329 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 330 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 332 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 337 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 331 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 338 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 338 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 329 — create or update the focused repair issue for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 328 completion record

~~~markdown
# Task 328 completion record

{{TASK_328_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 328 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 328's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 330 — commence the focused repair for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 329 completion record

~~~markdown
# Task 329 completion record

{{TASK_329_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 329 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 329 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 331 — implement the focused repair for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 330 completion record

~~~markdown
# Task 330 completion record

{{TASK_330_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 330 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 330 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 332 — create or update the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 332 PR URL | `{{TASK_332_PR_URL}}` | Produced by this task; replace {{TASK_332_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 331 completion record

~~~markdown
# Task 331 completion record

{{TASK_331_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 331 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 331 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 333 — run the Codex review loop on the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 332 PR URL | `{{TASK_332_PR_URL}}` | Produced by Task 332; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 332 completion record

~~~markdown
# Task 332 completion record

{{TASK_332_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 332 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 332 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 332's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 333 for the new head before Task 334. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 334 — run the independent final quality check on the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 332 PR URL | `{{TASK_332_PR_URL}}` | Produced by Task 332; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 333 completion record

~~~markdown
# Task 333 completion record

{{TASK_333_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 333 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 333 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 332's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 333 for the new head, and then repeat Task 334 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 335 — merge the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 332 PR URL | `{{TASK_332_PR_URL}}` | Produced by Task 332; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 334 completion record

~~~markdown
# Task 334 completion record

{{TASK_334_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 334 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 334 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 336 — publish the landed repair handoff for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 332 PR URL | `{{TASK_332_PR_URL}}` | Produced by Task 332; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 335 completion record

~~~markdown
# Task 335 completion record

{{TASK_335_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 335 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 335 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 337 — recheck the reciprocal result for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 328 completion record

~~~markdown
# Task 328 completion record

{{TASK_328_COMPLETION_RECORD}}
~~~

#### Task 336 completion record

~~~markdown
# Task 336 completion record

{{TASK_336_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 328 | `FS` | Use the original pinned comparison and branch result. |
| Task 336 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 322–336 as a new numbered-range instance. After its new handoff, repeat Task 337.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 338 — publish the fixed-point closure for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 337 completion record

~~~markdown
# Task 337 completion record

{{TASK_337_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 337 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 337 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 339 — check the independent residual triggers

> **Execution class: Coding agent executable.** This is a read-only trigger check and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| None | — | No technical predecessor. Run here to avoid interrupting the known capability cycles. Re-run when a listed input changes. |

Keep PS #155 and #156 open and independent. Do not add either issue as a blocker for PS #159, #152, #147, Terraform #21, or Terraform #22 without a new material trigger.

### Objective

Complete only this leaf action: check the independent residual triggers. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Trigger check:**

1. Re-query the current issues, repositories, supported hosts, native helpers, candidate files, and external runners.
2. Revisit PS #155 only if the candidate validator permits a competing writer, Windows PowerShell 5.1 support ends and a portable API becomes available, or a later task adopts a suitable native helper.
3. Revisit PS #156 only if the candidate files change or a suitable external runner becomes available.
4. Classify each trigger as `present` or `absent` and link its evidence.
5. If all triggers are absent, post a dated no-trigger record and stop.
6. If a trigger is present, record the affected issue and exact material evidence. Do not start implementation in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Each trigger has a current, evidence-backed classification and each present trigger identifies the focused residual work that Task 350 must evaluate.

### Complete when

Each trigger has a current, evidence-backed classification and each present trigger identifies the focused residual work that Task 350 must evaluate.

## Task 340 — assess one triggered residual cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | This disposition task reads both repositories |
| Selected repair target | `{{TASK_340_SELECTED_TARGET_REPOSITORY}}` | Produced by this task when repair is required; replace globally when known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 338 completion record

~~~markdown
# Task 338 completion record

{{TASK_338_COMPLETION_RECORD}}
~~~

#### Task 339 completion record

~~~markdown
# Task 339 completion record

{{TASK_339_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 339 | `FS` | A material trigger is documented. |
| Task 338 | `FS` | The preceding known fixed-point cycle is closed. |
| Prior completed instance of Tasks 333–350 | `FS` | Close one residual cycle before starting another. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 340 | Validate the trigger and complete the required finding-specific decision process. |
| Task 341 | Create one focused issue with only its real dependencies. Issue creation does not start implementation. |
| Task 342 | Verify the implementation slot is free. Explicitly start only that residual issue. |
| Task 344 | Prepare one focused PR and run the four task-local conditional repair PR lifecycle actions. |
| Task 340 | Record the complete landed identity, validation, review, and rollback evidence. |
| Task 340 | Do not change repository settings without exact administrator authorization. |
| Task 349 | If the other repository has the same role, compare the landed result there. |
| Task 343 | Create a reciprocal issue before implementation only when a common difference exists. |
| Task 342 | Start the reciprocal issue only after issue setup and a free implementation slot. Run the four task-local conditional repair PR lifecycle actions, then run the reverse comparison. |
| Task 350 | Close the cycle only when each common behavior is `same` or a proved intentional difference with equal security and failure strength. |
| Task 340 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 340 | Map files by role, not only by filename. |
| Task 340 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 343 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 341 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 342 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 344 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 349 | Compare the landed destination result back against the source repository. |
| Task 341 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 344 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 343 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 350 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 350 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 341 — create or update the focused repair issue for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_340_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 340; resolve globally before execution |
| Task 341 issue URL | `{{TASK_341_ISSUE_URL}}` | Produced by this task; replace {{TASK_341_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 340 completion record

~~~markdown
# Task 340 completion record

{{TASK_340_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 340 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 340's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 342 — commence the focused repair for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_340_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 340; resolve globally before execution |
| Task 341 issue URL | `{{TASK_341_ISSUE_URL}}` | Produced by Task 341; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 341 completion record

~~~markdown
# Task 341 completion record

{{TASK_341_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 341 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 341 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 343 — implement the focused repair for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_340_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 340; resolve globally before execution |
| Task 341 issue URL | `{{TASK_341_ISSUE_URL}}` | Produced by Task 341; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 342 completion record

~~~markdown
# Task 342 completion record

{{TASK_342_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 342 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 342 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 344 — create or update the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_340_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 340; resolve globally before execution |
| Task 341 issue URL | `{{TASK_341_ISSUE_URL}}` | Produced by Task 341; resolve globally before execution |
| Task 344 PR URL | `{{TASK_344_PR_URL}}` | Produced by this task; replace {{TASK_344_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 343 completion record

~~~markdown
# Task 343 completion record

{{TASK_343_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 343 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 343 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 345 — run the Codex review loop on the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_340_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 340; resolve globally before execution |
| Task 344 PR URL | `{{TASK_344_PR_URL}}` | Produced by Task 344; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 344 completion record

~~~markdown
# Task 344 completion record

{{TASK_344_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 344 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 344 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 344's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 345 for the new head before Task 346. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 346 — run the independent final quality check on the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_340_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 340; resolve globally before execution |
| Task 344 PR URL | `{{TASK_344_PR_URL}}` | Produced by Task 344; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 345 completion record

~~~markdown
# Task 345 completion record

{{TASK_345_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 345 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 345 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 344's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 345 for the new head, and then repeat Task 346 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 347 — merge the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_340_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 340; resolve globally before execution |
| Task 344 PR URL | `{{TASK_344_PR_URL}}` | Produced by Task 344; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 346 completion record

~~~markdown
# Task 346 completion record

{{TASK_346_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 346 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 346 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 348 — publish the landed repair handoff for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_340_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 340; resolve globally before execution |
| Task 344 PR URL | `{{TASK_344_PR_URL}}` | Produced by Task 344; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 347 completion record

~~~markdown
# Task 347 completion record

{{TASK_347_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 347 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 347 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 349 — recheck the reciprocal result for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 340 completion record

~~~markdown
# Task 340 completion record

{{TASK_340_COMPLETION_RECORD}}
~~~

#### Task 348 completion record

~~~markdown
# Task 348 completion record

{{TASK_348_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 340 | `FS` | Use the original pinned comparison and branch result. |
| Task 348 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 334–348 as a new numbered-range instance. After its new handoff, repeat Task 349.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 350 — publish the fixed-point closure for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 349 completion record

~~~markdown
# Task 349 completion record

{{TASK_349_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 349 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 349 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 351 — assess one future PS-first paired capability cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | This disposition task reads both repositories |
| Selected repair target | `{{TASK_351_SELECTED_TARGET_REPOSITORY}}` | Produced by this task when repair is required; replace globally when known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 338 completion record

~~~markdown
# Task 338 completion record

{{TASK_338_COMPLETION_RECORD}}
~~~

#### Task 350 completion record

~~~markdown
# Task 350 completion record

{{TASK_350_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 338 | `FS` | All currently known capability cycles are closed. |
| Every applicable Task 350 instance | `FS` | Triggered residual work is closed. |
| Prior completed instance of Tasks 344–361 | `FS` | The prior future cycle has a permanent fixed-point record. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 352 | Create or update the PS issue. Classify it as `common capability`, `repository-specific capability`, or `administrative only`. Issue creation does not start implementation. |
| Task 351 | Record landed inputs and blobs, intended paths, real native dependencies, expected differences, and any mapped Terraform issue. |
| Task 353 | Verify the implementation slot is free. Explicitly start only the PS implementation. |
| Task 355 | Prepare one PS PR and run the four task-local conditional repair PR lifecycle actions. Post a permanent handoff with exact landed identities and matrix rows. |
| Task 352 | Create or update the Terraform issue only from that landed PS handoff. Do not replace placeholders before the PS PR lands, and never use a branch, reviewed head, test-merge ref, or anticipated squash SHA as the landed commit. |
| Task 353 | After issue setup and a free implementation slot, explicitly start only the Terraform adaptation. Do not create a shared runtime dependency. |
| Task 351 | Run the four task-local conditional repair PR lifecycle actions for the Terraform PR. |
| Task 352 | Run the reverse PS comparison. Create a focused PS sync-back issue before any correction and start it only after issue setup and a free implementation slot. |
| Task 351 | If PS changes, compare Terraform again. Run the four task-local conditional repair PR lifecycle actions for any required Terraform repair. |
| Task 353 | Apply the row-direction stop rule. Repeat until the cycle reaches a fixed point. Do not start the next PS capability before the permanent closure record exists. |
| Task 351 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 351 | Map files by role, not only by filename. |
| Task 351 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 354 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 352 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 353 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 355 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 360 | Compare the landed destination result back against the source repository. |
| Task 352 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 355 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 354 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 361 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 361 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

#### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Codex review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 352 — create or update the focused repair issue for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_351_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 351; resolve globally before execution |
| Task 352 issue URL | `{{TASK_352_ISSUE_URL}}` | Produced by this task; replace {{TASK_352_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 351 completion record

~~~markdown
# Task 351 completion record

{{TASK_351_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 351 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 351's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 353 — commence the focused repair for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_351_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 351; resolve globally before execution |
| Task 352 issue URL | `{{TASK_352_ISSUE_URL}}` | Produced by Task 352; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 352 completion record

~~~markdown
# Task 352 completion record

{{TASK_352_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 352 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 352 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 354 — implement the focused repair for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_351_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 351; resolve globally before execution |
| Task 352 issue URL | `{{TASK_352_ISSUE_URL}}` | Produced by Task 352; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 353 completion record

~~~markdown
# Task 353 completion record

{{TASK_353_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 353 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Codex review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 353 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 355 — create or update the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_351_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 351; resolve globally before execution |
| Task 352 issue URL | `{{TASK_352_ISSUE_URL}}` | Produced by Task 352; resolve globally before execution |
| Task 355 PR URL | `{{TASK_355_PR_URL}}` | Produced by this task; replace {{TASK_355_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 354 completion record

~~~markdown
# Task 354 completion record

{{TASK_354_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 354 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 354 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 356 — run the Codex review loop on the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_351_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 351; resolve globally before execution |
| Task 355 PR URL | `{{TASK_355_PR_URL}}` | Produced by Task 355; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 355 completion record

~~~markdown
# Task 355 completion record

{{TASK_355_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 355 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Codex review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 355 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 355's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 356 for the new head before Task 357. Do not merge.

### Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 357 — run the independent final quality check on the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_351_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 351; resolve globally before execution |
| Task 355 PR URL | `{{TASK_355_PR_URL}}` | Produced by Task 355; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 356 completion record

~~~markdown
# Task 356 completion record

{{TASK_356_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 356 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 356 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 355's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 356 for the new head, and then repeat Task 357 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 358 — merge the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_351_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 351; resolve globally before execution |
| Task 355 PR URL | `{{TASK_355_PR_URL}}` | Produced by Task 355; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 357 completion record

~~~markdown
# Task 357 completion record

{{TASK_357_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 357 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 357 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 359 — publish the landed repair handoff for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_351_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 351; resolve globally before execution |
| Task 355 PR URL | `{{TASK_355_PR_URL}}` | Produced by Task 355; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 358 completion record

~~~markdown
# Task 358 completion record

{{TASK_358_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 358 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 358 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 360 — recheck the reciprocal result for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 351 completion record

~~~markdown
# Task 351 completion record

{{TASK_351_COMPLETION_RECORD}}
~~~

#### Task 359 completion record

~~~markdown
# Task 359 completion record

{{TASK_359_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 351 | `FS` | Use the original pinned comparison and branch result. |
| Task 359 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

#### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Codex review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 345–359 as a new numbered-range instance. After its new handoff, repeat Task 360.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 361 — publish the fixed-point closure for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 360 completion record

~~~markdown
# Task 360 completion record

{{TASK_360_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 360 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 360 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 362 — verify final completion

> **Execution class: Coding agent executable.** This is a read-only final audit and evidence-publication task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 339 completion record

~~~markdown
# Task 339 completion record

{{TASK_339_COMPLETION_RECORD}}
~~~

#### Task 350 completion record

~~~markdown
# Task 350 completion record

{{TASK_350_COMPLETION_RECORD}}
~~~

#### Task 361 completion record

~~~markdown
# Task 361 completion record

{{TASK_361_COMPLETION_RECORD}}
~~~

#### Completed history and Tasks 1–338 aggregate completion records

~~~markdown
# Completed history and Tasks 1–338 aggregate completion records

{{COMPLETED_HISTORY_AND_TASKS_001_TO_338_COMPLETION_RECORDS}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Completed history and Tasks 1–338 | `FS` | Every known task and reciprocal cycle is complete. |
| Every applicable Task 350 and Task 361 instance | `FS` | All triggered residual and in-scope future cycles are complete. |
| Task 339 | `FS` | The latest residual-trigger record is current. |

### Objective

Complete only this leaf action: verify final completion. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Codex review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Completion audit:**

Verify all of the following:

- Every implementation had a focused issue record and a separate explicit commencement record. No two feature implementation issues or PRs were active at the same time.
- Every cross-repository input was pinned to a landed commit and blob. No work copied from a moving branch or introduced a shared runtime dependency, reusable cross-repository workflow, submodule, package, or third source of truth.
- Every PR completed a discrete Codex review loop and then a discrete independent fresh-agent quality check against the same final head SHA and tree before merge. Every permanent cycle record contains both gate results and both repositories' issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, paths, blobs, validation, runtime identities, and review outcomes.
- Every merged PR passed the whole-PR comment and deferral audit. No unfinished reviewer finding or illegitimate deferral remains. Every legitimate deferral has a cited, self-contained GitHub issue with correct native dependencies.
- Every merged PR title and body accurately describe its final landed diff, issue scope, validation, security effects, intentional differences, deferred-issue links, and rollback information.
- Every required reciprocal matrix contains each applicable row exactly once. Every common row is `same`. Every intentional difference names both behaviors, repository need, equal security and failure strength, owner, and review or expiry condition. No blocker remains.
- Every posted issue body and comment contains verified identities. No placeholder or fabricated SHA, URL, PR number, tree, or blob remains.
- Every native dependency represents a real prerequisite. No tracker falsely blocks implementation, and no setting changed without exact administrator authorization.
- Initial cycles 1 through 5 closed in order with their Terraform comparisons, required repairs, reverse PS comparisons, and permanent fixed-point records.
- PS #159 and its four children are closed with a permanent landed handoff that names both repositories' final commits and blobs.
- PS #163 closed without duplicating volatile protocol text.
- Terraform #31 contains all five initial cycle records and closed without an aggregate foundation implementation PR.
- PR #164 was reviewed and landed through PS #160, and its reciprocal cycle reached a fixed point.
- The generators share the canonical tracked-present/tracked-absent publication and structured-result behavior.
- Both generators use fixed root/destination authority, complete BOM-less UTF-8/LF payloads, same-directory durable candidate verification, one `File.Replace` or non-overwriting `File.Move` publication call, truthful `ReplacementStateUncertain` reporting, no post-publication rollback claim, and reusable raw NUL-delimited Git validation with exact native-status handling.
- Repository-code jobs in both repositories contain no actions and have no repository-token scopes.
- Both repositories use separate action-only publication jobs, verified official Node distributions in action-free Node jobs, strict offline policy parsing and fixtures, machine-readable authority for roles, inputs, defaults, supply identities and case allocation, and the same script-version grammar and PowerShell authoring rules.
- Both repositories have the same common workflow-policy behavior and case coverage, except for recorded repository-specific literals. No workflow writer existed before P1B/T1B introduced and proved it.
- PS #158 is complete, and both repositories have a read-only, reproducible supply-freeze method.
- Terraform #21 closed from landed PS PR #153 after the reverse PS comparison and all sync-back work.
- The global sweep inventoried all common roles, resolved every untracked blocker through focused cycles, and retained final two-repository evidence.
- PS #147 and #152 are complete, Terraform #22 consumed the landed P1B handoff, and the P1B/T1B cycle has no blocker.
- PS #148 and Terraform #23 each have a landed result and the required cross-repository applicability record.
- PS #149 landed after the deadline-path decision. Terraform #24 consumed its handoff after #23. The P3/T3 cycle reached a fixed point.
- PS #151 has an applicable Terraform follow-up or a pinned structural non-applicability record.
- The advisory decision did not expire without completed PS #149 or a separately approved, fully pinned superseding decision.
- PS #155 and #156 remained independent unless a documented trigger created a focused blocker.
- Every later common capability used a landed PS handoff, a Terraform adaptation, a reverse PS comparison, and fixed-point closure before the next PS implementation.
- No shared cross-repository runtime dependency exists.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Post one permanent final audit that identifies the final PS and Terraform commits and trees, links every fixed-point record, lists all validation and runtime evidence, records every intentional difference and non-applicability result, and states that no blocker remains.

### Complete when

Post one permanent final audit that identifies the final PS and Terraform commits and trees, links every fixed-point record, lists all validation and runtime evidence, records every intentional difference and non-applicability result, and states that no blocker remains.

## References

- [PS issue #145](https://github.com/franklesniak/PSStyleGuide/issues/145) and [PS PR #150](https://github.com/franklesniak/PSStyleGuide/pull/150)
- [PS issue #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153)
- PS issues [#147](https://github.com/franklesniak/PSStyleGuide/issues/147), [#148](https://github.com/franklesniak/PSStyleGuide/issues/148), [#149](https://github.com/franklesniak/PSStyleGuide/issues/149), [#151](https://github.com/franklesniak/PSStyleGuide/issues/151), [#152](https://github.com/franklesniak/PSStyleGuide/issues/152), [#155](https://github.com/franklesniak/PSStyleGuide/issues/155), [#156](https://github.com/franklesniak/PSStyleGuide/issues/156), and [#158](https://github.com/franklesniak/PSStyleGuide/issues/158)
- [PS foundation umbrella #159](https://github.com/franklesniak/PSStyleGuide/issues/159), child issues [#160](https://github.com/franklesniak/PSStyleGuide/issues/160), [#161](https://github.com/franklesniak/PSStyleGuide/issues/161), and [#162](https://github.com/franklesniak/PSStyleGuide/issues/162), and [merged PR #164](https://github.com/franklesniak/PSStyleGuide/pull/164)
- [PS issue #166](https://github.com/franklesniak/PSStyleGuide/issues/166) and [merged PR #165](https://github.com/franklesniak/PSStyleGuide/pull/165)
- [PS Claude-command issue #163](https://github.com/franklesniak/PSStyleGuide/issues/163)
- [PS #161 draft PR #167](https://github.com/franklesniak/PSStyleGuide/pull/167), initial [generator-check failure](https://github.com/franklesniak/PSStyleGuide/actions/runs/31856644635/job/94942553633), initial [policy-check failure](https://github.com/franklesniak/PSStyleGuide/actions/runs/31856644627/job/94942553647), final-head [generator-check success](https://github.com/franklesniak/PSStyleGuide/actions/runs/31869046641/job/94974664264), and final-head [Markdown-check success](https://github.com/franklesniak/PSStyleGuide/actions/runs/31869046647/job/94974664365)
- [Terraform issue #20](https://github.com/franklesniak/TerraformStyleGuide/issues/20), [PR #26](https://github.com/franklesniak/TerraformStyleGuide/pull/26), [PR #27](https://github.com/franklesniak/TerraformStyleGuide/pull/27), and [PR #30](https://github.com/franklesniak/TerraformStyleGuide/pull/30)
- Terraform issues [#21](https://github.com/franklesniak/TerraformStyleGuide/issues/21), [#22](https://github.com/franklesniak/TerraformStyleGuide/issues/22), [#23](https://github.com/franklesniak/TerraformStyleGuide/issues/23), [#24](https://github.com/franklesniak/TerraformStyleGuide/issues/24), and [#31](https://github.com/franklesniak/TerraformStyleGuide/issues/31)
- [Terraform issue #32](https://github.com/franklesniak/TerraformStyleGuide/issues/32) and [merged PR #33](https://github.com/franklesniak/TerraformStyleGuide/pull/33)
- [GitHub secure-use guidance for Actions](https://docs.github.com/en/actions/reference/security/secure-use)
- [GitHub Actions runner authentication design](https://github.com/actions/runner/blob/main/docs/design/auth.md)
- [GitHub Actions `GITHUB_TOKEN`](https://docs.github.com/en/actions/concepts/security/github_token)
- [GitHub workflow permissions](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#permissions)
- [Node.js permission model](https://nodejs.org/api/permissions.html)
- [.NET `File.Replace`](https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace)
- [.NET `File.Move`](https://learn.microsoft.com/en-us/dotnet/api/system.io.file.move)
- [Git pathname format and `-z`](https://git-scm.com/docs/git-status#_pathname_format_notes_and_z)
- [GitHub issue dependencies](https://docs.github.com/en/issues/tracking-your-work-with-issues/using-issues/creating-issue-dependencies)
- [OpenAI Codex subagents](https://learn.chatgpt.com/docs/agent-configuration/subagents)
- [OpenAI Codex non-interactive mode](https://learn.chatgpt.com/docs/non-interactive-mode)
- [OpenAI Codex `AGENTS.md` instructions](https://learn.chatgpt.com/docs/agent-configuration/agents-md)
- [Anthropic Claude Code Windows Bash defect #28333](https://github.com/anthropics/claude-code/issues/28333)
- [Anthropic Claude Code setup and update guidance](https://code.claude.com/docs/en/setup)
- [Anthropic Claude Code installation troubleshooting](https://code.claude.com/docs/en/troubleshoot-install)
- [PS #168 — PR-body identity synchronization](https://github.com/franklesniak/PSStyleGuide/issues/168)
- [PS #169 — BSD identity dispatch and candidate-publication binding](https://github.com/franklesniak/PSStyleGuide/issues/169)
- [PS #170 — culture-invariant rationale anchors](https://github.com/franklesniak/PSStyleGuide/issues/170)
- [PS #171 — bounded field-specific generator diagnostics](https://github.com/franklesniak/PSStyleGuide/issues/171)
- [Merged PS PR #167](https://github.com/franklesniak/PSStyleGuide/pull/167) and [landed PS #161 handoff](https://github.com/franklesniak/PSStyleGuide/issues/161#issuecomment-5334317038)
- [Merged Terraform PR #35](https://github.com/franklesniak/TerraformStyleGuide/pull/35) and [landed Terraform #34 handoff](https://github.com/franklesniak/TerraformStyleGuide/issues/34#issuecomment-5336853959)
